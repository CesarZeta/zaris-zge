# ESTADO — zaris-zge (backoffice + backend)

> **Hilo conductor común Cesar ↔ Roy.** Este archivo es la **fuente de verdad compartida y versionada** del estado de avance. Es lo PRIMERO que se lee al retomar y lo que se ACTUALIZA al cerrar cada sesión (regla CLAUDE.md §45). Es un documento **vivo y corto** — foto del estado actual, NO bitácora histórica (esa vive en [`HISTORIAL_MIGRACIONES.md`](HISTORIAL_MIGRACIONES.md) y, para Cesar, en su memoria privada de Claude Code).
>
> **Reglas de mantenimiento:**
> - Actualizar al cerrar sesión (la skill `/cierre-sesion` lo hace antes de tocar la memoria privada).
> - "En curso" y "Pendientes" reflejan lo REAL — si terminás algo, movelo a "Hecho reciente" (y podá lo viejo de ahí: máx ~10 líneas).
> - Convertir fechas relativas a absolutas.
> - Verificar contra git/prod antes de declarar algo hecho (`feedback_verificar_siempre_antes_de_opinar`).
> - La PWA App Vecinos tiene su **propio** `ESTADO.md` en el repo `zaris-vecinos`. Acá va solo backoffice/backend.

**Última actualización:** 2026-09-20 (Cesar): sesión de estado + decisiones. **(0) BÚSQUEDA DIFERIDA (§23) EXTENDIDA A TODA LA SUITE** — estándar por decisión de César: 13 pantallas de listado (Reclamos, OT ×3, Trámites ×2, Agenda ×3, Entradas, Encuestas envíos, Ciudadanos/Empresas listado) ya no piden datos al entrar; helper compartido `web-app/src/ui/busqueda.tsx`; alcance y exclusiones (tableros, BI, inicio, Emergencias, catálogos, search-first) en CLAUDE.md §23; verificado pantalla por pantalla en local + typecheck + build prod (detalle en Pendientes → ítem tachado). **(1) QA visual de ATENCIÓN CERRADO por César** — F3 colero, F4 Guardia, F5 Historia clínica y F6 BI de atención dieron OK en prod: **el proyecto Atención (6 fases, `PLAN_MODULO_ATENCION.md`) queda CERRADO**; residuos en Pendientes (ocupación vs disponibilidad, export PDF, `registra_atencion`). **(2) Purga del historial git HECHA** (autorizada por César): `git filter-repo` con callback quirúrgico — redactó la clave de testing `123456` SOLO en versiones históricas (259 blobs, 744 ocurrencias); el árbol de `main` quedó byte a byte idéntico (mismo tree `5afab9f…`), 633 commits, `main`/`cloudflare/workers-autoconfig` force-pusheados y verificados por la API de GitHub. **TODOS los SHAs anteriores cambiaron**: mapa viejo→nuevo en [`docs/commit-map-purga-2026-09-20.txt`](docs/commit-map-purga-2026-09-20.txt); los SHAs citados en `*.md` del repo (skills, memorias curadas, ESTADO, HISTORIAL, planes) y en la memoria privada ya están reencaminados. Quien tenga un clon viejo debe re-clonar (`git fetch` + `git reset --hard origin/main`). Residuo que NO depende de nosotros: GitHub conserva los commits viejos accesibles por SHA directo y por los refs de los PRs #1-#3 hasta que Support los purgue (opcional: pedirlo en support.github.com; las claves ya estaban muertas por el cambio forzado del 2026-07-18). Lo que en el historial parecía una `SUPABASE_SERVICE_KEY` era el placeholder de 23 caracteres de `.env.example`, y el `DISPATCHER_TOKEN` hallado también era placeholder — el de prod vive solo en Railway y no entró en la purga. Backup completo pre-purga: `backups/zaris-zge-pre-purge-2026-09-20.bundle` (local, gitignored). **(3) Gobierno con turnos:** disponibilidad L-V 08:00-16:00 cargada en prod al agente 89 (`disponibilidad_recurso` 23) para la prestación 8 "INSCRIPCION PROFESIONAL" (verificado por API: aparece en la mesa del lunes 21 en Mesa de Atencion Municipal); local no tiene esa prestación. **(4) Planilla `credenciales-testing/` actualizada** (médicos 99/100, Pestto n4/74, Kramer n5, `cesar@` con clave propia). **(5) Decisiones de César:** QA de modo oscuro POSPUESTO (faltan cosas por desarrollar) · **Roy no está activo en el desarrollo por ahora** (el ítem de Vercel queda en pausa; los avisos para la PWA los toma César) · **próxima sesión: São Paulo + "la ubicación"** · pidió repasar la regla de búsqueda diferida antes de decidir extenderla. Sesión anterior (2026-09-19, 2ª parte): **GENERADOR DE DATOS DEMO DE ATENCIÓN HECHO y PROD CARGADA** (`backend/app/services/demo_atencion.py`, commits `bc78746` + `ea2f539`; mismo usuario `generador.demo`, mismos vecinos demo, mismo endpoint `POST /demo/poblar` con `modulos=[reclamos,atencion]` y mismo cron semanal `demo-datos.yml`, que desde ahora genera también la semana de atención). Backfill de prod abril → 19/09 en 4 runs del workflow con `modulos=atencion` (runs 35474180124 / 35474253338 / 35474331341 / 35474384879, todos `success` + HTTP 200): **2.943 turnos demo** (450-525/mes, 128 reservados hasta el 26/09), 179 derivaciones a la Guardia atendidas por los médicos demo, 45 eventos con 3.234 reservas; reclamos demo intactos (2.376). Integridad verificada en prod: 0 solapes de ocupaciones, 0 números de colero repetidos, 0 llamados de otro día, 0 encuestas pendientes, 0 sobrecupos. Idempotente por recurso/día (turnos), por día (Guardia) y por rango (eventos): re-generar un rango ya poblado agrega ~0. Detalle en la skill `modulo-bi` ("Datos demo de ATENCIÓN"). 1ª parte del día: **ATENCIÓN F6 — BI de atención por gestión HECHA y EN PROD** (commit `89b5382` + dist CI `522e34d`; Railway expone las 19 rutas `/bi/atencion/*` y GH Pages sirve `index-D46ZcUkF.js`; verificado por API en prod con `administrativo@` n2: score = 32 turnos, matriz, guardia 0 derivaciones, eventos 9, export con `X-Total-Count`, 401 sin token) (tercer tablero de DATOS, ruta `/bi/atencion` + tarjeta en la landing; backend `bi_atencion.py` con 19 rutas `/api/v1/bi/atencion/*`, **sin migración**; 6 secciones Resumen / Evolución / Ubicaciones / Espera / Guardia / Eventos con filtros período + gestión + ubicación + prestación; smoke `smoke_bi_atencion.py` 85/85 + typecheck + verificación navegando en `localhost:5173`; criterios en la skill `modulo-bi`). **Con esto las 6 fases del plan de Atención están hechas.** Queda: QA visual de César en prod tras el deploy y **decidir un generador de datos demo de turnos/guardia/eventos** (el tablero se ve ralo: ~26 turnos local, ~32 prod — ver Pendientes). Sesión anterior (2026-09-13): **ATENCIÓN F5 Historia clínica EN PROD** (migs **107** log de accesos append-only `historia_clinica_acceso` + **107b** clave `id_area_salud` por nombre del área activa "Secretaría de Salud", aplicadas en `zaris_dev`; `GET /turnos/atenciones/historia` + `/permiso` con guard Salud + nivel 1 resuelto por `agentes.id_subarea` (fail-closed, 403 accionable ANTES de la BUC) y registro de TODO acceso incluidos los denegados; `HistoriaClinica.tsx` en el detalle del turno, las mesas del colero y de la Guardia, Atender en Guardia y Consultas, gateado por `/permiso`; smoke 61/61 + typecheck verde). **Prod HECHO el 2026-09-13:** preflight §24 OK (una sola área Salud activa = 57; `configuracion_general.tipo/activo` NOT NULL sin default, sin CHECK de `tipo`) → 107 y 107b aplicadas por conexión directa (el MCP no estaba) y verificadas (tabla, triggers, RLS, clave `'57'`) ANTES del push → commit `9f6ad22` + dist CI `eee9140` (GH Pages sirve `index-CzYsMqlh.js`) → marker OpenAPI en Railway en ~1,5 min → verificación por API en prod: `/permiso` como `medico1.guardia@` = `{puede:true, motivo:'salud'}`, como `administrativo@` (n2 fuera de Salud) = `fuera_salud` + 403 accionable, historia del Vecino Demo 200 (total 0), id inexistente 404, sin token 401; las 3 primeras filas del log de prod son exactamente esas llamadas. **Queda tu QA visual en prod** (ver Pendientes). Siguiente: F6 (BI de atención). El 2026-09-12 se verificó el cron de trámites (no hizo nada, esperado), se re-sembraron 10 turnos demo para el lunes 14 y se crearon los médicos de guardia demo 99/100 (ver Pendientes). Sesión anterior (2026-09-06, 3ª): **ATENCIÓN F4 Guardia EN PROD** (migs 106 + 106b: `emergencia_atencion`, subárea "Emergencias" bajo Secretaría de Salud, espacio "Guardia"; "Derivar a la Guardia" en el detalle del evento del COM sin tocar su FSM; mesa de la Guardia en Turnos con Atender / No se presentó; smoke 23/23). 2ª sesión del 06: **Búsqueda diferida en Turnos** (regla de producto nueva en CLAUDE.md §23: ninguna pantalla de listado pide datos al entrar — filtros + leyenda + botón Buscar), **colero cerrado** (chime con botón "Activar sonido"; prefijo `ODO-001` de 3 alfanuméricos cargable desde Agenda → Config → Espacios; prefijos demo cargados en prod), **definición de F4** (Guardia bajo Secretaría de Salud, subárea "Emergencias") y turnos demo sembrados en prod para el QA del colero. El "Failed to fetch" que César vio en Prestaciones era **LOCAL con el uvicorn caído** (la captura mostraba la ubicación "Sala de Atencion Central", que solo existe en `zaris_dev`), no prod. Sesión anterior del mismo día (1ª): **(1) Trámites — configuración + agrupación**: el ciclo de vida de la mig 101 quedó ACTIVO (6 estados `espera_iniciador` + SLA 2-4 días en los 15 tipos, local y prod) y se hizo explícita la **gestión responsable del tipo** (mig **104** `tipo_tramite.id_subarea`; área 1 renombrada a «Secretaría de Gobierno»; las 7 subáreas del circuito bajo ella; `seed_tramites.py` corregido para que el ordenamiento sea determinístico). **(2) ATENCIÓN F3 — colero** (mig **105**): FSM `reservado → llamado → cumplido | ausente`, panel de atención en la Mesa del día y **pantalla pública de sala** `/pantalla/:token` con polling 5 s. **(3) Higiene**: color de marca a verde `#1f8a65`, y rotación del `DISPATCHER_TOKEN` **descartada por decisión de César**. Bugs corregidos el 06: token no-UUID daba **500** en un endpoint público (ahora 404) y un filtro `fecha` que FastAPI ignoraba en silencio. Pendientes de definición: São Paulo, IT-01/08/09, áreas fijas, **export PDF "Prestaciones realizadas" de Consultas** (saca texto clínico sin el guard de la HC — quitar / gatear / dejar).

---

## 🔵 En curso

- **🔵 ATENCIÓN POR UBICACIÓN (Turnos reorganizados + colero + guardia + historia clínica + BI por gestión)** — plan aprobado por César 2026-09-01, 6 fases en [`PLAN_MODULO_ATENCION.md`](PLAN_MODULO_ATENCION.md) (decisiones cerradas: ubicación = `espacios_agenda`; gestión = área; **las atenciones derivadas de Emergencias NO van con turno** — registro propio `emergencia_atencion`; historia clínica mínima viable restringida a Salud; colero público tokenizado con "número + nombre e inicial"). **F1 + F2 + F2b HECHAS, verificadas y PUSHEADAS 2026-09-01 (cierre)**: mig 103 aplicada en local Y prod (`tipo_prestacion.id_espacio_ubicacion` + `turnos.id_espacio_ubicacion` copiada al reservar en las 3 vías + backfill + índice) · **2ª decisión de César**: se mantienen turnos por agente Y por espacio, pero la **ubicación es OBLIGATORIA** en toda prestación (422 backend + form; demo backfilleada, 0 activas sin ubicación en local ni prod) · **el módulo Turnos ahora se navega ubicación-primero**: landing de Ubicaciones agrupada por gestión/área con contadores del día (`GET /turnos/ubicaciones`), **Mesa del día** por ubicación (grilla espacio + agentes con disponibilidad efectiva batch + ocupaciones; turnos del agente en OTRA ubicación enmascarados desde el backend; guard subárea nivel 3-4) (`GET /turnos/ubicaciones/{id}/mesa`), contexto persistente (`zaris_turnos_ubicacion`) que scopea Turnos/Agenda/Atendidos vía filtro backend `id_espacio_ubicacion`. **F2b también HECHA el mismo día**: el módulo AGENDA suma la pill "Por ubicación" (primera del toggle) con select agrupado por gestión — la grilla Día/Semana muestra el espacio + sus agentes (backend `id_espacio_ubicacion` en `/agenda/calendario` y `/semana`, helper `_recursos_de_ubicacion`, compat retro verificada; el select consume `GET /turnos/ubicaciones` por URL directa, sin import cross-módulo). Smokes 16/16 + 15/15 + 10/10 PASS + verificación visual completa en localhost:5173. ~~**OJO deploy:** re-correr el backfill de la 103 por los turnos creados en el medio~~ **NO HACE FALTA — verificado en prod 2026-09-06:** el código ya está deployado (`/turnos/ubicaciones` y `/ubicaciones/{id}/mesa` en el OpenAPI de Railway) y no se crearon turnos entre la mig y el deploy (el último turno de prod es del 2026-07-06). Prod hoy: **0 prestaciones activas sin ubicación** y **0 turnos futuros sin ubicación**; los 8 turnos sin ubicación son legacy por agente y ya pasados (el residuo conocido de F2). **F3 colero (mig 105) y F4 Guardia (migs 106 + 106b) HECHAS y EN PROD 2026-09-06; F5 Historia clínica (migs 107 + 107b) EN PROD 2026-09-13** (`GET /turnos/atenciones/historia` = `turno_atencion` ∪ `emergencia_atencion` atendida|ausente por ciudadano; guard Salud + nivel 1 por `agentes.id_subarea` y clave `id_area_salud`; log de accesos append-only; UI gateada por `/permiso`; verificado en prod por API, ver "Última actualización"). **F6 BI de atención por gestión HECHA 2026-09-19** (`/bi/atencion`, sin migración, smoke 85/85 — ver "Última actualización"). **Las 6 fases del plan están hechas y el generador demo de atención ya cargó prod (2.943 turnos, 179 derivaciones, 45 eventos): el proyecto pasa a QA visual en prod.** Decisiones por fase en el plan.

> ### 📣 AVISO PARA ROY (PWA `zaris-vecinos`) — 3 ENDPOINTS NUEVOS que la PWA ya puede consumir (2026-08-30, EN PROD)
> Cierran tres `TODO(backend)` que dejaste en el código de la PWA. Contrato completo en [`docs/contrato_api_publica.md`](docs/contrato_api_publica.md) §4 / §5 / §10 / §11; todos con JWT scope `publico`. **Nada de lo existente cambió de contrato** (solo se sumó `avisos.no_leidos` a `/mi-resumen`).
> 1. **`GET /api/v1/publico/perfil`** → ficha completa (`telefono, calle, altura, localidad, provincia, latitud, longitud, fecha_nac, nacionalidad, cuil, email_verificado, fecha_alta…`) para `DatosPersonales.tsx` (tu interfaz `PerfilExtendido` ya tiene los mismos nombres). **`cuil` viene `null` + `cuil_es_placeholder=true`** cuando es el placeholder del alta por agente — podés borrar el chequeo `20+DNI+9` del front. **`PUT /publico/perfil`** = edición parcial de contacto/domicilio (`telefono` → 10 dígitos sin 0, se guarda digit-only; `calle/altura/localidad/provincia/latitud/longitud`). Email e identidad NO se editan desde la app (decisión pendiente de César).
> 2. **`GET /api/v1/publico/avisos`** → `{avisos, no_leidos, total}` para `AlertasPage.tsx` (hoy estado vacío). Cada aviso trae `url` relativa (`/reclamos/123`, `/emergencias`) para navegar al tocarlo. `PATCH /avisos/{id}/leer` y `POST /avisos/leer-todos`. El badge de la campana sale de `GET /portal/mi-resumen` → `avisos.no_leidos` (sin request extra). Los avisos los genera el backend en cada cambio de estado de reclamo/reporte (los mismos que disparan el push) — quedan aunque el vecino no tenga push activo.
> 3. **`GET /api/v1/publico/reclamos/geo/reverse?lat=&lon=`** → reemplaza la llamada directa a Nominatim de `src/lib/geo.ts::direccionDesdeCoords`. Devuelve siempre 200: usar `direccion` (corta, "Calle Altura, Localidad"); si `encontrado=false` caer a `direccionGps()` como hoy.
> 4. **Foto de perfil (tu propuesta del 2026-07-06, aprobada por César y EN PROD desde 2026-08-30, mig 102):** `POST /api/v1/publico/perfil/foto` (multipart, campo `archivo`, **solo JPEG**, 1 KB..512 KB, ≥100×100 px — la reducción a 256 px que ya hace `src/lib/perfil.ts` alcanza) y `DELETE /publico/perfil/foto`. `GET /publico/perfil` trae `foto_url` (URL **firmada, TTL 1 h** — refrescar el perfil en vez de cachear la URL) y `foto_actualizada_en`. Reemplaza el guardado en `localStorage` (`zaris_vecino_foto`): la foto ahora sigue al vecino entre dispositivos. Contrato §10.
>
> ### 📣 AVISO PARA ROY (PWA `zaris-vecinos`) — 2 CAMBIOS DE CONTRATO en alta de empresa (2026-07-15)
> **1) `POST /api/v1/publico/alta/empresa` (el que usa la PWA) — se cerró un IDOR.** Era anónimo y tomaba `id_ciudadano` del body → cualquiera podía vincular una empresa a un vecino ajeno. **Ahora exige el vecino logueado** y el representante sale del JWT, no del body.
> - **La PWA DEBE mandar `Authorization: Bearer <token scope publico>`** al llamar a `/alta/empresa`. Sin token → **401**.
> - **`id_ciudadano` ya NO va en el body** (se ignora; el backend usa el ciudadano del token). Quitalo del payload.
> - El resto del contrato (`municipio_slug`, `cuit`, `razon_social`, `id_actividad`, domicilio, contacto…) queda **igual**.
> - Ya estaba previsto por diseño (2026-06-09: "la empresa la carga el vecino logueado desde el portal"), así que si tu request ya iba autenticado, solo sacá `id_ciudadano`. Si iba anónimo, autenticalo.
>
> **2) Regla de negocio reforzada: toda empresa nace con un vecino.** Esto NO afecta a la PWA (el vecino logueado ES el representante), pero por si tocás el alta de empresa del backoffice: `POST /api/v1/buc/empresas` (agente) **ahora exige `id_ciudadano` + `id_tipo_representacion`** y crea empresa + vínculo en una transacción. Sin representante → **422**. El endpoint `POST /buc/ciudadano-empresa` sigue existiendo pero ya no hace falta llamarlo tras el alta (el vínculo se crea solo).

---

## 🟠 Pendientes (backoffice / backend)

> El grueso del trabajo abierto del proyecto hoy está en la **PWA** (ver `zaris-vecinos/ESTADO.md`). Acá quedan cosas de backoffice/infra. **Roy no está activo en el desarrollo por ahora (César, 2026-09-20):** los ítems de la PWA (consumir perfil / avisos / geo-reverse / foto, mocks visibles en prod, prueba en celular) quedan para César + Claude Code cuando se retome la PWA; el "AVISO PARA ROY" de arriba sigue siendo la guía técnica.

- **🟠 PRIMERO AL ARRANCAR LA PRÓXIMA SESIÓN (pedido de César 2026-09-20): PODAR este `ESTADO.md`.** Pesa 62 KB y la regla §45 dice "documento vivo y corto". Qué podar: el bloque "Última actualización" (una pared de texto acumulada — dejar solo la foto de la última sesión, 5-10 líneas; lo anterior ya vive en Hecho reciente, `HISTORIAL_MIGRACIONES.md` y los `PLAN_*.md`), los ítems tachados de Pendientes que ya no aportan contexto (mover lo útil a Hecho reciente y borrar el resto), y el bloque "En curso" del proyecto Atención (cerrado: reducir a una línea que apunte al plan). Hacerlo al inicio, con la sesión fresca, y verificar contra git/prod antes de borrar cualquier "pendiente" (`feedback_verificar_pendientes_antes_de_atacar`). Después de la poda: São Paulo + "la ubicación" (ver más abajo).
- ~~🔴 VERIFICACIÓN PENDIENTE (2026-09-07): primera corrida real del cron de trámites con el ciclo de vida ACTIVO~~ **VERIFICADA 2026-09-12 contra la DB de prod:** el cron corrió 6 veces (07 al 12, todas `success`) y **no hizo nada, como se esperaba** — 0 movimientos `aviso_iniciador`/`desistido`, 0 trámites con `resultado='desistido'`, 0 con `desist_aviso_nivel > 0`. Los 2 expedientes con mail real (`HAB-LPL-2026-0001`, `CAR-LPL-2026-0002`) siguen con el reloj en cero (primer aviso recién a +30 días, ~2026-10-06). También verificado el cron demo-datos del lunes 07: corrió solo (demorado por GitHub a 14:16 UTC, patrón conocido) y dejó 01-07/9 uniforme (9-18 reclamos/día, sin capa doble); 08-12 vacíos porque el próximo run es el lunes 14.
- ~~🟡 QA visual de ATENCIÓN F3 + F4 + F5 + búsqueda diferida en PROD (César)~~ **QA CERRADO por César 2026-09-20 ("QA de F3, 4 y 5 OK") — con esto el proyecto Atención queda cerrado; el recorrido de abajo queda como guía de re-test.** Estado previo (re-verificado 2026-09-20 contra la DB de prod, antes del OK): 0 `turno_llamado`, 0 `emergencia_atencion` y 0 accesos a HC que no sean del generador demo o de la verificación por API del 13/09; los turnos 32-41 del lunes 14 vencieron en `reservado`. **Ya NO hace falta re-sembrar:** el generador demo de atención dejó turnos `reservado` para toda la semana del 21 al 25/09 (Sala Municipal de Odontologia 10-16 por día, Consultorio Municipal 8-10, SUM 2-4), así que el recorrido de abajo se puede hacer cualquier día hábil con esos turnos. ~~datos re-sembrados 2026-09-12 para el LUNES 14.~~ Los turnos demo del 07 vencieron sin usarse (verificado: 0 `turno_llamado`, 0 `emergencia_atencion`, 0 turnos futuros en prod al 12). Se crearon por API (`administrativo@`) **10 turnos `reservado` el 2026-09-14** en la **Sala Municipal de Odontologia** (ids 32-41: 4 de la prestación del espacio 08:00-10:15 + 6 del agente 08:00-09:30 y 14:00-14:30; ocupaciones espejo 10/10). No se sembró para el sábado 12 porque la disponibilidad es lunes-viernes y `turnos_respeta_disponibilidad=true` → 409. A mirar el lunes: Turnos → Ubicaciones → Sala Municipal de Odontologia → Mesa del día → panel "Atención del día" (Llamar / Re-llamar / Atendido / Ausente; el número sale **`ODO-001`**; campo Puesto) y **"Abrir pantalla"** (nivel ≤2) → la TV en `/pantalla/:token` con **"Activar sonido"**. Guardia (no depende de turnos, se puede probar cualquier día): Emergencias → detalle de un evento → "Derivar a la Guardia" → Turnos → Ubicaciones → card Guardia → Atender / No se presentó. Búsqueda diferida: las tabs Turnos/Agenda/Atendidos/Prestaciones muestran la leyenda y cargan recién al Buscar. **Historia clínica (F5, sumada 2026-09-13; exige las migs 107/107b + el backend nuevo en prod):** entrar como **`medico1.guardia@` (n4 Salud, subárea 82)** — al primer login pide cambiar la clave (`debe_cambiar_password`). (a) Mesa de la **Guardia** → botón **"Historia clínica"** en la fila pendiente que tenga paciente BUC (la fila "Sin identificar" no lo muestra) → modal con encabezado del paciente y timeline (o "Sin atenciones registradas" si todavía no tiene); al **Atender**, el `<details>` "Historia clínica del paciente" dentro del modal. (b) Después de marcar **Atendido** con intervención en el colero de Odontología y de **Atender** en la Guardia al MISMO ciudadano demo: Turnos → **Consultas** → buscar ese ciudadano → 3ª solapa **"Historia clínica"** → los dos ítems (Guardia con badge dorado arriba, Turno con badge verde) y "Cambiar" vuelve a la solapa Turnos; detalle de un turno atendido → 3ª solapa (carga recién al clickearla). (c) Como un usuario de nivel 2-4 **fuera de Salud** ni el botón ni las solapas aparecen (fail-closed por `/permiso`); como admin sí. (d) Config → Sistema → sección **"Atención"** muestra `id_espacio_guardia` = 9 e `id_area_salud` = 57. Cada apertura deja fila en `historia_clinica_acceso` (consultar por SQL: no hay pantalla).
- ~~🟡 QA visual de ATENCIÓN F6 (BI de atención) en PROD (César)~~ **QA CERRADO por César 2026-09-20 ("QA de BI OK")** sobre el deploy `89b5382` + dist `522e34d` (GH Pages sirve `index-D46ZcUkF.js`, verificado ese día). ~~Decisión pendiente sobre datos demo~~ **DECIDIDO por César 2026-09-19: generador demo de atención sincronizado con el de reclamos — HECHO** (`backend/app/services/demo_atencion.py`, mismo usuario `generador.demo`, mismos vecinos demo, mismo endpoint `POST /demo/poblar` con `modulos=[reclamos,atencion]` y mismo cron semanal; detalle en la skill `modulo-bi`). Queda abierto "ocupación vs disponibilidad efectiva" (hoy ocupación = turnos + horas atendidas por ubicación, sin % sobre la agenda disponible).
- **🟡 VERIFICACIÓN PENDIENTE (César, cuando pueda): recorrido en PROD (iframe real del shell) de las 13 pantallas con búsqueda diferida** (Reclamos, OT ×3, Trámites ×2, Agenda ×3, Entradas, Encuestas → Envíos, Ciudadanos/Empresas → Listado). Está deployado y verificado en local con el mismo patrón que César ya aprobó en Turnos; falta solo la mirada humana en prod. Atención especial a: Agenda (barra de fecha + "Ver agenda" antes de la grilla; después navegar fechas re-pide), bandeja de Trámites (Buscar deja `?buscar=1` en la URL; un deep link con filtros arranca buscado) y el botón "Listado" de Ciudadanos/Empresas (ya carga sin gate).
- **🟡 VERIFICACIÓN PENDIENTE (lunes 2026-09-21): primera corrida del cron `demo-datos.yml` con el default `reclamos,atencion`.** Esperado en el JSON del run (Actions → "Datos demo BI"): `generado` de reclamos como siempre (semana 15-21/09, sin capa doble) y `generado_atencion` con `recurso_dia_ya_demo` / `dias_omitidos_con_demo` > 0 (los días 15-19/09 ya los cargó el backfill: la idempotencia debe saltearlos) + turnos nuevos solo del 20-21/09 y de los 7 días siguientes. Si el cron llega tarde, NO re-dispatchar (regla del incidente 2026-08-31); verificar el run tardío y los conteos por día.
- ~~🟠 Dato de negocio para César (cazado en el backfill de prod 2026-09-19): la gestión "Secretaría de Gobierno" no tiene turnos de ATENCIÓN demo en prod~~ **RESUELTO 2026-09-20 (decisión de César: "está muy bien que haya turno para Inscripción Profesional aunque por ahora sea el único"):** disponibilidad L-V 08:00-16:00 cargada en prod al agente 89 (`disponibilidad_recurso` id 23, mismo horario que el resto de los agentes demo); verificado por API que aparece en la mesa del lunes 21/09 de Mesa de Atencion Municipal con 0 ocupaciones. El cron demo del 21/09 debería empezar a generarle turnos; para histórico, re-dispatchar el rango con `modulos=atencion` (idempotente por recurso/día). Local no tiene esa prestación. Hallazgo original: Su única prestación de atención ("INSCRIPCION PROFESIONAL", agente 89 = Cesar Zarini) no tiene disponibilidad cargada, así que ni el generador ni una reserva real pueden tomar slots (solo entran las reservas del SUM, que son `reserva_espacio`). Opciones: cargarle disponibilidad al agente 89 (Agenda → Config → disponibilidad) o crear otra prestación de Gobierno con un agente disponible; el cron semanal la empieza a poblar solo desde ese momento (para el histórico habría que re-dispatchar el rango con `modulos=atencion`, que es idempotente por recurso/día).
- ~~🟢 Verificaciones que quedaron SIN hacer en F5 (2026-09-13, no son bugs)~~ **cubiertas por el QA visual de César en prod (iframe real) del 2026-09-20; queda solo el caso 429 del smoke con `--rate` si alguna vez hace falta.** Detalle original: la HC se navegó completa en `localhost:5173` (n4 Salud, n3 fuera de Salud, admin, **dark mode OK**), pero **no** se miró en el iframe real del shell (`localhost:8080` / prod) ni el ancho de la columna Acciones del colero con los 4 botones en pantallas angostas; el caso 429 del smoke corre solo con `--rate`. Al primer login en la UI, `medico1/2.guardia@` piden cambiar la clave (`debe_cambiar_password`) — es lo esperado, no un error.
- ~~🟠 Médicos de guardia — bloqueado por dato de negocio~~ **HECHO 2026-09-12 (decisión de César: "creá los médicos demo").** Hallazgo previo: en prod **no había ningún agente activo en la Secretaría de Salud** (área 57: Odontología 78, Clínica médica 79, Emergencias 82 → 0 agentes; el odontólogo Saavedra, agente 1, está en la subárea 8). Se crearon **2 médicos de guardia DEMO** replicando por SQL el alta real (`POST /buc/usuarios` interno = usuario + agente 1:1 en una tx; vínculo como `POST /agenda/espacios/9/agentes`): usuarios **99 `medico1.guardia@municipio.gob.ar`** y **100 `medico2.guardia@municipio.gob.ar`** (nivel 4 Gestión, subárea 82 Emergencias/Salud, clave estándar de testing con **`debe_cambiar_password=TRUE`** → al primer login la UI pide elegir una nueva, igual que las 12 cuentas de la auditoría), agentes **95/96 "Guardia Demo, Médico 1/2"**, vínculos `espacio_agentes` 10/11 al espacio Guardia (9). Verificado por API en prod: login 200 como nivel 4 y **ve la mesa de Guardia** (`/turnos/ubicaciones/9/mesa` 200, `/guardia/atenciones` 200). No hay cargo "Médico" en `cargos` (queda NULL). ~~**Pendiente menor:** sumar las 2 cuentas a la planilla de `credenciales-testing/` (junto con la corrección de Pestto).~~ **HECHO 2026-09-20** (planilla actualizada: médicos 99/100, Pestto n4 / Espacios Verdes 74, Kramer n5, `cesar@` con clave propia; copia previa en `credenciales-testing/_backup/`). Las áreas "Salud" duplicadas (5, 7, 33) están inactivas — no es problema. **F5 (2026-09-13):** estos dos médicos demo (subárea 82 Emergencias, bajo el área 57 que apunta la clave `id_area_salud` de la 107b) **son los que pasan el guard de la historia clínica en prod** (`GET /turnos/atenciones/historia/permiso` → `{puede:true, motivo:'salud'}` — a confirmar en la verificación post-deploy); los usuarios `f5*@municipio.gob.ar` del smoke existen **SOLO en local**. Un agente activo de Odontología (78) o Clínica médica (79) también pasaría; el odontólogo Saavedra NO (subárea 8, fuera de Salud) — es dato de negocio, no código.
- ~~🟡 Colero — 2 definiciones abiertas~~ **CERRADAS por César 2026-09-06 e implementadas el mismo día** (ver Hecho reciente): chime con botón "Activar sonido"; prefijo = 3 alfanuméricos + `-` + correlativo (`ODO-001`), cargable en Agenda → Config → Espacios.
- ~~🟢 F4 (guardia) — lista para arrancar~~ **HECHA y EN PROD 2026-09-06 (3ª sesión)** — ver Hecho reciente. **Queda para César: QA visual de F4 en prod** (Emergencias → detalle de un evento → "Derivar a la Guardia"; Turnos → Ubicaciones → card Guardia → mesa → Atender / No se presentó) y **vincular a los médicos de guardia** como agentes del espacio Guardia (Agenda → Config → Espacios → Agentes) para que los usuarios de nivel 3-4 de Salud vean la mesa (hoy la ven admin/supervisor y quien tenga subárea "Emergencias"). ~~**Siguiente fase: F5 historia clínica**~~ **F5 HECHA 2026-09-13** (ver Hecho reciente; prod en la misma sesión). **Siguiente: F6 BI de atención.**
- ~~🟠 Búsqueda diferida (regla §23 nueva) — extenderla a los demás módulos~~ **HECHO 2026-09-20 — es ESTÁNDAR DE LA SUITE por decisión de César** ("aplicarlo como si fuera un estándar para toda la suite en lo que respecta a endpoints que requieren de una búsqueda para listar información"; excluye tableros, BI, inicio con mapa y las pantallas operativas de Emergencias/call center). Aplicado en 13 pantallas: Reclamos (listado), OT (Supervisor/Agente/Auditoría), Trámites (bandeja con filtros en la URL + mi bandeja), Agenda (grillas con "Ver agenda", eventos, conflictos), Entradas, Encuestas (envíos), Ciudadanos y Empresas (listado completo; el botón "Listado" del buscador llega ya buscado por `state.buscar`). Helper promovido a `web-app/src/ui/busqueda.tsx`. Verificado en `localhost:5173` pantalla por pantalla (0 requests de datos al entrar, request + tabla al clic; capturas de Agenda y OT) + typecheck + build de prod. Alcance y exclusiones definitivas en CLAUDE.md §23. Los 4 subagentes que lo arrancaron murieron por límite de sesión de la API a mitad de camino: se completó a mano a partir de sus ediciones parciales. Decisión de producto de César 2026-09-06: ninguna pantalla de listado pide datos al entrar. Aplicada en Turnos; **Reclamos, OT, Trámites, Emergencias y Agenda siguen precargando** y se migran módulo por módulo al tocarlos (patrón de referencia: `web-app/src/modules/turnos/lib/busqueda.tsx`). Toda pantalla de listado NUEVA nace con el patrón.
- **🟢 Trámites — 2 residuos conocidos:** (a) las transiciones finales de los seeds usan `tipo_accion='avanzar'` en vez de `aprobar`/`rechazar` (solo 6 en todo el catálogo lo tienen), así que el **resultado automático + la encuesta CSAT** todavía no se disparan solos en la mayoría de los tipos — se puede mapear por estado destino si César quiere; (b) los 4 tipos sin circuito (`exención de tasas`, `permiso de espacio público`, `arbolado urbano`, `inscripción profesional`) quedaron con una gestión asignada **por criterio mío**, cambiable desde el builder en 10 segundos.

- **🟢 Fase 3 roles — EN PROD (2026-07-02).** El push + rebuild dist ya se hicieron (commits `c8639c2` feat roles + `e4c93ed` dist, ancestros de `origin/main` — verificado 2026-07-17); solo queda (d) revisar scoping de Turnos/Trámites. Modelo de niveles nuevo (mig 92, YA aplicada en local Y prod): 1=Admin · 2=Supervisor (scopeado subárea) · 3=**Atención** (ve todo, ex Operador) · 4=**Gestión** (NUEVO — agente de OT, listados scopeados a su subárea) · 5=Consultor (los 41 ex-nivel-4 ya renumerados en prod). Ver CLAUDE.md §3. Hecho: scoping bandeja OT + guards de asignación + picker de agentes + listado/stats de reclamos + búsqueda por nro/rango/fechas en la mesa + "Avisar al supervisor" (acción sugerida + comentario, fallback a admins, anti-spam 30 min, notif in-app + mail) + fixes (POST /reclamos sin guard de nivel ni tipo obligatorio). **Pendiente:** (a) ~~`git push` backend + rebuild dist~~ **HECHO 2026-07-02** (commits `c8639c2` + `e4c93ed`); (b) ~~reclasificar usuarios~~ **HECHO 2026-07-02**: subárea "Call Center" creada (bajo Gobierno); quedan en Atención (3) SOLO los 2 con cargo "Operador de Call Center" (elizabethcarpio, mariaacosta — movidos a Call Center) + `operadorcom` (excepción: Emergencias/COM exige nivel ≤ 3, §44); los otros 31 pasaron a Gestión (4). Backups: `_backup_usuarios_niveles_2026_07_02` + `_backup_agentes_subarea_2026_07_02`. **2026-07-02 (2ª pasada, aprobada por Cesar):** las 10 jefaturas (Director/Subdirector/Coordinador/Subsecretaria) promovidas a Supervisor (2). Prod queda: 3 admin · 22 sup · 3 atención · 21 gestión · 41 consultor. Demo congelada REC-58/59 switcheada a involucrados coherentes: OT-2026-000011 → agente `chiarauncal` (n4, Señalamiento Vial, pass testing estándar), supervisor `josewaisbord` (n2, cargo nuevo 'Supervisor Operativo', movido a Señalamiento Vial); `lourdesacosta` (n2, Supervisor Operativo) movida a Alumbrado Público para poder cerrar REC-58 (marisaiturriaga volvió a Luminaria-interno). Guion en `credenciales-testing/guion_gestion_reclamos.html` actualizado (fila agente B + continuación); (c) verificar en prod post-deploy; (d) ~~Gestión de Turnos/Trámites: revisar scoping~~ **CERRADO 2026-07-18** (`6a92cf8`): las mutaciones de Turnos scopean igual que la lectura y Trámites sumó gate anti-Consultor + guard de comentar + nivel 2 a su colectivo en tomar/liberar/resultado (resto del bypass nivel-2 diferido, ver el ítem de auditoría abajo). **Fase 3 roles COMPLETA.**
- **🟠 Config/UX — pendientes de la reunión 06/07/2026 (decisiones de César, NO resueltos en la sesión del 15/07).** Los ajustes acotados (IT-02..06) ya están en prod (ver Hecho reciente). Quedan abiertos, requieren definición tuya:
  - **IT-01 Multi-tenant + super administrador + selector de municipio** — es un PROYECTO de arquitectura, no una tarea. Hoy es mono-tenant (`id_municipio` NULL, §38/§43). La "identidad" del municipio está repartida entre la tabla `municipios` (nombre/provincia/país/`codigo_corto`) y `configuracion_general` (nombre visible del topbar/logo/color) — consolidarla en una ficha única es la base de IT-01. Diseñar por fases con César antes de tocar código.
  - **IT-08 Botón SOS (App Vecinos)** — aprobado a nivel diseño; falta que César defina QUÉ acción dispara antes de conectarlo a backend.
  - **IT-07 Accesibilidad app móvil** (tipografía grande, botones auto-explicados) — toca la PWA `zaris-vecinos` (otro repo → su `ESTADO.md`).
  - **IT-09 Módulo de Obras** — backlog, sin alcance definido.
  - Informe original: PDF adjunto en la tarea "Informe Bug" de la lista **CONFIGURACION y UX** de ClickUp (queda `en curso` a propósito por estos pendientes).
- ~~🟡 BI Ejecutivo~~ **HECHO 2026-08-30** (`c393e92`, EN PROD — ver Hecho reciente). ~~Queda su QA visual~~ **QA CERRADO 2026-09-01**: César navegó prod, reportó los hallazgos de la 5ª tanda (2 vueltas, corregidos en el día — ver Hecho reciente 2026-09-01) y dio el **OK final** ("TODO VERIFICADO OK"). El detalle acumulado de las tandas 2ª-5ª vive en la skill `modulo-bi`. Datos demo abr-ago poblados (agosto = 462 tras la limpieza del 2026-08-31).
- **🟠 Áreas fijas por regla de negocio (definición de César, planteado 2026-08-30):** determinadas áreas/secretarías (Seguridad, Salud, Servicios Públicos…) deberían venir **precargadas por default y NO ser removibles** del sistema (renombrables sí), porque atienden funciones básicas de la plataforma directamente vinculadas a módulos (Emergencias→Seguridad, Turnos/prestaciones→Salud, Reclamos→Servicios Públicos). NO requiere cambio de código todavía: es un requerimiento a ESTABLECER en las reglas de negocio y analizar en todo el contexto (seeds, admin_tablas, baja lógica, multi-tenant IT-01). César lo quiere ver después de cerrar los tableros ejecutivos.
- ~~🟡 QA visual del BI Operativo en prod — 3ª vuelta tras la 2ª tanda de ajustes (`83b220b`)~~ **CERRADO 2026-08-30**: César navegó el Operativo en prod (bundle `index-WC5OyJfy.js` = dist CI `e9f5224`, verificado) y dio el OK a los 7 puntos: índice correcto al click y al scroll, mapa de Pendientes bajo la barra fija, panel de filtros contraíble con resumen, meses sin tilde, total al centro de las donas, doble separación entre visualizaciones y fila de KPIs en una línea.
- ~~🟡 QA visual post-sesión 2026-07-18~~ **CERRADO 2026-07-19**: César navegó y dio el OK visual (tablero de pánico, pastillas BI en dark, labels "Entradas" en Encuestas). El sub-ítem (d) re-corrida de `smoke_emergencias.ps1` se ejecutó contra local el 2026-07-19 (ver Hecho reciente).
- **🟡 Barrido visual modo oscuro — POSPUESTO por César 2026-09-20 ("faltan cosas para desarrollar"; retomar cuando lo pida)** de módulos React restantes — solo Dashboard y Guías verificados en dark; queda el repaso VISUAL navegando (QA). Los 6 literales claros conocidos quedaron resueltos el 2026-07-18 (`5ab2900`): las 4 pastillas de gráficos BI pasaron a tokens invertibles `--fg-1`/`--surface-100`, y los 2 casos de texto sobre color fijo (Recepción de Emergencias, botón primario de Identidad) quedaron documentados como intencionales (caso avatar §13). Vanilla (admin_tablas) tiene el bootstrap pero sin verificación visual dark.
- **🟠 Latencia por región — migrar API + DB a São Paulo (`sa-east-1`). PRÓXIMA SESIÓN (César, 2026-09-20): resolver São Paulo + "la ubicación" (a precisar con César al arrancar).** Verificado 2026-07-15: el proyecto Supabase `ZARIS GESTION ESTADO` (`lshfwsscvfsklrmbvkwl`) está en **`us-east-1`** (Virginia, hemisferio norte); la org ya tiene otro proyecto en `sa-east-1` (`zaris-news-bot`), así que la región destino está probada. Parte del piso de latencia documentado (~2-3s Railway↔Supabase) es geografía. **Falta verificar la región de Railway** (no la vimos aún): la latencia percibida = Railway↔Supabase + cliente↔Railway; mudar solo la DB puede no ganar nada si Railway sigue en US — el objetivo es que **backend Y DB** queden en `sa-east-1`. Mover un proyecto Supabase de región NO es un toggle: implica proyecto nuevo en `sa-east-1` + migrar datos + repuntar connection strings/env vars → **sesión dedicada con backup y ventana**, no un cambio al pasar. Pedido de César 2026-07-15.
- ~~🟡 Encuestas para TRÁMITES — sin soporte~~ **HECHO 2026-08-30** (mig 101+101b, ver Hecho reciente): se disparan cuando el trámite termina con resultado aprobado/rechazado (decisión de César).
- ~~🟠 CONFIGURAR EL CATÁLOGO DE TRÁMITES para que el ciclo de vida hacia el vecino ACTÚE~~ **HECHO 2026-09-06 (lo aplicó Claude Code por DB, en local Y prod — pedido de César: "¿no podés hacerlo vos?").** Antes: 0 estados con `espera_iniciador` y 0 tipos con SLA → el motor estaba encendido (`tramite_desistimiento_activo=true`) pero inerte. Ahora en **prod**: (a) `espera_iniciador=TRUE` en los **6 estados donde la pelota es del vecino** — `subsanacion` de Habilitación comercial (17) y de Permiso Demostración E2E (68/73/82), `pago_pendiente` de Cartel publicitario (40), `subsanar` de Inscripción Profesional (89); los estados de trabajo interno (revisión/evaluación/inspección) quedaron en FALSE a propósito. (b) **SLA por tipo en los 15 tipos activos**: 2, 3 o 4 días (`2 + id_tipo_tramite % 3`, pseudo-aleatorio determinístico alrededor de 3, decisión de César). (c) ⚠️ **Los 2 expedientes vivos que caían bajo el timer** (`HAB-LPL-2026-0001` y `CAR-LPL-2026-0002`, ambos de empresa con **email real de Gmail**, parados hace 122 y 130 días = pasados los 90 del desistimiento) **tenían el reloj a cero** (`fecha_entrada_estado_actual=NOW()`): sin eso, el cron diario les mandaba 4 mails reales y los **desistía automáticamente en ~4 corridas**. Ahora el primer aviso recién a los +30 días. Backup de TODOS los valores previos en la tabla `_backup_ciclo_vida_2026_09_06` de prod. **En local** se aplicó lo mismo (2 estados de espera + 9/9 tipos con SLA) SIN resetear relojes, para poder probar el timer. **Queda abierto (no se tocó, es semántica del circuito):** las transiciones finales de los seeds usan `tipo_accion='avanzar'` en vez de `aprobar`/`rechazar` (solo 6 de todo el catálogo lo tienen), así que el **resultado automático + la encuesta CSAT** todavía no se disparan solos en la mayoría de los tipos. Plazos globales en Config → Sistema: aviso 1 +30 días, aviso 2 +60, último aviso 72 h antes, desistido a +90.
- ~~🟡 Branding App Vecinos: cargar `municipio_color_primary` en prod~~ **HECHO 2026-08-30, cambiado a VERDE el 2026-09-06 por pedido de César**: hoy vale **`#1f8a65`** (el verde del DS, `--color-success`, que además era el default histórico de la PWA antes del branding) — verificado en `GET /publico/identidad-municipio` de prod y aplicado también en local. El azul anterior `#1e5aa8` queda en `_backup_ciclo_vida_2026_09_06`. El acento sigue `#0fd7ff`. Cuando César defina el color de marca definitivo se cambia desde **Config → Identidad** (sin código).
- ~~🟢 Doc: el prefijo `ALERTA DE PANICO` no figura en `docs/contrato_api_publica.md`~~ **HECHO 2026-08-30**: nota bajo la tabla de §8 Emergencias del contrato (prefijo, normalización strip/mayúsculas/sin tildes, efectos en backoffice, `GET /eventos` no expone el flag; el body no puede setear `es_panico`, por diseño).
- **🟡 Vercel: deploys de Roy quedan BLOCKED (autor fuera del team) — EN PAUSA: Roy no está activo en el desarrollo por ahora (César, 2026-09-20).** Mitigado: `git merge --no-ff` al integrar sus ramas (regla en CLAUDE.md de `zaris-vecinos`) o commit vacío de re-trigger. Fix de raíz (decisión de César): sumarlo al team en el dashboard (asiento Pro facturado). **Verificado 2026-08-30 (Vercel MCP):** el team `ZARIS TEAM WEB` (`cesar-zarinis-projects`) está en plan **Hobby**, que no admite miembros → sumar a Roy implica pasar el team a Pro (facturación por asiento; confirmar precio vigente en el dashboard). Es un cambio de facturación que solo César puede hacer; mientras tanto sigue el `merge --no-ff`.
- ~~🟢 Higiene: rotar `DISPATCHER_TOKEN` (visible en historial git)~~ **DESCARTADO POR DECISIÓN DE CÉSAR (2026-09-06):** "eso queda así, no pasa nada". No re-proponerlo. (El token lo usan los 3 crons: encuestas, trámites, integridad; el secret sobrante `ZARIS_DISPATCHER_TOKEN` también queda como está.)
- **🟢 Credenciales de prod expuestas — mitigado y LIMPIADO (auditoría 2026-07-18).** La password universal `123456` de 12 cuentas (incl. admins `cesar@` id6 y `roymanos@` id3) estaba publicada en el repo público (guías QA `docs/qa_*.html`, skills, smokes). Se forzó `debe_cambiar_password=true` en las **12** (backup `_backup_debe_cambiar_pwd_2026_07_18` en prod) **y las credenciales se SACARON de todos los artefactos trackeados** (`a6c81fc`): smokes/seeds prod-capaces leen `ZARIS_QA_PASS`/`ZARIS_SEED_*` de env (default dev solo local), guías QA y docs referencian `credenciales-testing/`. **Nota para Roy:** si tu login te pide cambiar la clave, es por esto — normal; y para correr smokes contra prod ahora seteás `ZARIS_QA_PASS`. ~~**Pendiente vivo (decisión de César):** evaluar purga del historial git~~ **PURGA HECHA 2026-09-20** (ver "Última actualización": `git filter-repo` quirúrgico sobre versiones históricas, árbol de `main` intacto, mapa de SHAs viejo→nuevo en `docs/commit-map-purga-2026-09-20.txt`; residuo fuera de nuestro control: los commits viejos siguen accesibles en GitHub por SHA directo y por los refs de los PRs #1-#3 hasta que Support los purgue).
- ~~🟠 Auditoría de seguridad — último residuo (bypass nivel-2 de Trámites)~~ **CERRADO 2026-07-19** (ver Hecho reciente) — **la auditoría 2026-07 queda 100% completa** (48/48 hallazgos resueltos, refutados o aceptados con decisión). Derivado de DATOS demo — **RESUELTO 2026-08-30**: el agente 91 Juan Pestto (usuario `juanpesto` id 2, nivel 4 Gestión, sin OTs/trámites/aprobaciones/turnos/equipos) pasó de Censos (63) a **Espacios Verdes (74)** (`agentes.id_subarea` y `usuarios.id_subarea` a 74; Censos queda con 2 agentes activos). Con eso los 3 `poda-arbol` vivos ya puede avanzarlos ese usuario, no solo un admin. Revertir = volver ambos campos a 63. Actualizar la planilla de `credenciales-testing/` (lo lista como "Operador" sin subárea). Las demás subáreas usadas por circuitos/visados están pobladas (verificado en prod: 68=1, 72=1, 73=1, 70=2, 1=3, 69=3, 71=4 agentes). Residuos conscientes documentados en la skill `modulo-tramites` (catálogo admin sigue nivel ≤2, lectura sin scope, re-resolver visados).

---

## ✅ Hecho reciente (últimas sesiones)

- **2026-09-20 — BÚSQUEDA DIFERIDA (§23) COMO ESTÁNDAR DE LA SUITE — EN PROD (`4c7e615` + dist CI `24921a4`, GH Pages sirve `index-YwhghPq4.js`).** 13 pantallas de listado dejan de precargar: Reclamos, OT ×3, Trámites ×2 (bandeja con la URL como filtros aplicados), Agenda ×3 (grillas con "Ver agenda"), Entradas, Encuestas envíos, Ciudadanos/Empresas listado (botón "Listado" del buscador llega ya buscado). Helper compartido `web-app/src/ui/busqueda.tsx`; hooks con `opts { enabled, version }`. Verificado en `localhost:5173` pantalla por pantalla (fetch interceptado: 0 requests de datos al entrar, request + tabla al clic; capturas de Agenda y OT), typecheck y build prod. Exclusiones definitivas en CLAUDE.md §23. **Mismo día — ATENCIÓN CERRADO (QA de César OK en F3/F4/F5/F6) + PURGA DEL HISTORIAL GIT + Gobierno con disponibilidad + planilla de credenciales al día.** Purga: `git filter-repo` con callback que redactó `123456` solo en blobs fuera del árbol de `main` (259 blobs / 744 ocurrencias; HEAD intacto, tree `5afab9f…`), force push de `main` (`a7533a3` → `5631714`) y `cloudflare/workers-autoconfig`, verificación por API de GitHub, SHAs citados en `*.md` y memorias reencaminados con el mapa `docs/commit-map-purga-2026-09-20.txt`, bundle pre-purga en `backups/`. Disponibilidad L-V 08:00-16:00 al agente 89 (prestación 8 "INSCRIPCION PROFESIONAL", prod). Decisiones: dark mode pospuesto, Roy inactivo por ahora, próxima sesión São Paulo + ubicación.
- **2026-09-19 — ATENCIÓN F6: BI DE ATENCIÓN POR GESTIÓN + GENERADOR DE DATOS DEMO DE ATENCIÓN — EN PROD (cierra las 6 fases del plan).** **Tablero** `/bi/atencion` (tercera tarjeta de DATOS; `bi_atencion.py` con 19 rutas, sin migración; 6 secciones Resumen / Evolución / Ubicaciones / Espera / Guardia / Eventos; filtros período + gestión + ubicación + prestación; exports sin datos personales) — commits `89b5382` + dist `522e34d` + `0173720`, smoke `smoke_bi_atencion.py` 85/85, navegado en local, API de prod verificada. **Generador** `demo_atencion.py` (decisión de César el mismo día: "sincronizado con el de reclamos") — commits `bc78746` + `ea2f539` + `88bf553`: turnos dentro de la disponibilidad efectiva con ocupación espejo, colero, llamados, atenciones y CSAT; Guardia (eventos de Emergencias completos + `emergencia_atencion`); eventos con reservas; `POST /demo/poblar` con `modulos=[reclamos,atencion]` + cron semanal + `seed_demo_bi.py --modulos`; idempotente por recurso/día, día y rango. **Prod cargada** abril → 19/09 (4 runs `success`): 2.943 turnos, 179 derivaciones, 45 eventos / 3.234 reservas, reclamos demo intactos, integridad 0 en todos los chequeos. Criterios en la skill `modulo-bi`; plan en `PLAN_MODULO_ATENCION.md`.
- **2026-09-13 — ATENCIÓN F5: HISTORIA CLÍNICA MÍNIMA VIABLE (migs 107 + 107b) — EN PROD.** **Migraciones** 107 (log de accesos `historia_clinica_acceso`, append-only con triggers no-update/no-delete + RLS, sin FK a `ciudadanos` para registrar también los ids inexistentes) + 107b (clave `configuracion_general.id_area_salud` sembrada **por nombre** del área activa "Secretaría de Salud": 56 local / 57 prod) aplicadas en local; en prod van ANTES del push del backend (sin la tabla, cada consulta de la HC daría 500). **Backend:** `GET /turnos/atenciones/historia?id_ciudadano=&contexto=&limit=&offset=` une `turno_atencion` (mig 86) y `emergencia_atencion` atendida|ausente (mig 106; la `pendiente` es cola de mesa, no historia) con `origen` y orden cronológico normalizado a UTC-3, más `GET …/historia/permiso` para la UI (misma función que el guard). **Guard Salud + nivel 1** (`services/historia_clinica.py`): nivel 1 siempre; 2-4 solo si `agentes.id_subarea` (regla 1:1 §39, nunca `usuarios.id_subarea`) cuelga del área de la clave con área+subárea+agente activos; 5 nunca; clave ausente / sin agente / sin subárea = **fail-closed** con 403 accionable evaluado **antes de tocar la BUC**. **Todo acceso queda registrado** — también los 403 y 404 — dentro de la misma transacción y con commit antes de responder (sin fila no hay respuesta); rate limit `hc:{id_usuario}` 60/min. `/turnos/atenciones` conserva su scope genérico (`HistorialAtenciones`/`CumplirTurnoModal` intactos). **Frontend:** `HistoriaClinica.tsx` (Panel embebible / Modal a nivel página / Botón, los tres **gateados SOLO por `/permiso`**) desde el detalle del turno (3ª solapa lazy), las mesas del colero y de la Guardia (botón por fila + modal), Atender en Guardia (`<details>` cerrado) y Consultas (3ª solapa); `ApiError.status` en `lib/api.ts` (retrocompatible) para el 403 amigable; sección "Atención" en Config → Sistema (`id_espacio_guardia` + `id_area_salud`). Decisiones: nivel 5 y áreas "Salud" inactivas excluidos; ciudadano dado de baja SÍ muestra su historia (con aviso); sin export PDF de la HC; sin flag en login/`/me`; nunca apilar dos `Modal` de Agenda. Bug cazado por el smoke: `id_ciudadano` > int32 daba 500 y el intento no quedaba en el log → cota `le=2147483647` (422). **Verificado:** smoke in-process local **61/61** (`backend/smoke_atenciones_historia.py`: 401, n1, Salud n2/n3/n4, n5 incl. Consultor de Salud, fuera de Salud, área inactiva, sin agente, clave inactiva con restauración, endpoint viejo intacto, greedy, lista negra de campos, log con delta exacto, append-only, marker OpenAPI; `--rate` para el 429) + `pnpm typecheck` 0 errores + navegación en local. Fixtures `f5*@municipio.gob.ar` SOLO en local (se conservan como demo). Docs: `PLAN_MODULO_ATENCION.md` (F5 HECHA + §2.4 + pendientes de etapa 2), `HISTORIAL_MIGRACIONES.md` (107/107b), skills `modulo-turnos-entradas` (sección HISTORIA CLÍNICA) y `modulo-emergencias`, CLAUDE §21 (**próxima mig 108**). **Para César:** queda tu decisión sobre el export PDF "Prestaciones realizadas" de Consultas (saca intervención/recomendaciones sin el guard de la HC — ver Pendientes del plan §4).

- **2026-09-06 (3ª sesión) — ATENCIÓN F4: GUARDIA + DERIVACIÓN DESDE EMERGENCIAS (migs 106 + 106b) — EN PROD.** César eligió F4 al cierre de la 2ª sesión. **Migraciones** en local Y prod (`apply_migration`, las dos): `emergencia_atencion` (pendiente → atendida | ausente; paciente BUC o nombre libre — el denunciante no es necesariamente el paciente; UNIQUE "una pendiente por evento"; CHECK atendida ⇒ intervención) + los tipos `DERIVACION_GUARDIA`/`ATENCION_GUARDIA` en el CHECK del log; seed **por nombre** (área de Salud es 56 local / 57 prod): subárea **"Emergencias"** bajo Secretaría de Salud (115 local / 82 prod), espacio **"Guardia"** (12 local / 9 prod, prefijo GUA) y clave `id_espacio_guardia`. **Emergencias:** botón "Derivar a la Guardia" en el detalle (cualquier estado salvo DESESTIMADO, también RESUELTO; se oculta mientras hay una pendiente) → `POST /eventos/{id}/derivar-guardia` con motivo obligatorio y paciente (denunciante por defecto o "es otra persona"); **no cambia el estado del evento** (decisión: sin turno de por medio, el móvil sigue su ciclo). **Turnos:** la Guardia aparece en Ubicaciones con badge GUARDIA y "N derivaciones pendientes"; su mesa muestra el panel de derivaciones (pendientes de cualquier día + cerradas del día, refresco 30 s) con **Atender** (intervención obligatoria, recomendaciones, vínculo opcional a BUC) y **No se presentó**. El log del evento recibe la atención **sin el detalle clínico** (lo lee el COM). Guard de la mesa de Guardia = el de cualquier mesa (nivel ≤2 todo; 3-4 solo subárea Emergencias o agente vinculado; 404 genérico). Servicio compartido `app/services/guardia.py`. **Verificado:** smoke local 23/23 (derivar, 409 duplicado, 422 desestimado/motivo vacío/intervención vacía, atender, 409 re-atender, ausente, re-derivar tras cerrar, listado y filtro, log con los 2 tipos y sin la intervención, 404 id ajeno, scope nivel 3 fuera de Salud → 404 en la mesa pero SÍ deriva su evento) + navegación en local (landing, mesa, modal Atender con toast, detalle de Emergencias con el modal de derivación). Docs: `PLAN_MODULO_ATENCION.md` (F4 HECHA), `HISTORIAL_MIGRACIONES.md` (106/106b), skills `modulo-emergencias` y `modulo-turnos-entradas`, CLAUDE §21 (próxima mig 107).

- **2026-09-06 (2ª sesión) — BÚSQUEDA DIFERIDA EN TURNOS + COLERO CERRADO (chime + prefijo) + DEFINICIÓN DE F4 — EN PROD.** Arrancó con el estado del proyecto (infra toda verde) y el QA de César sobre Turnos: *"el solo acceso a la pantalla no tiene que hacer un requerimiento sin haberlo solicitado"*. **(1) Búsqueda diferida** (nueva regla de producto, CLAUDE.md §23): las tabs **Turnos, Agenda, Atendidos, Prestaciones y Mis turnos** muestran los filtros con la leyenda "Elegí qué querés ver y presioná Buscar" y recién ahí piden al backend; cambiar un filtro no dispara nada; Refrescar/Exportar deshabilitados hasta la primera búsqueda; Enter = Buscar. Se mantienen cargando solas **Ubicaciones** (la landing ES el selector) y la **Mesa del día con ubicación elegida** (opciones recomendadas, aprobadas por César con "P1 lo resolvés vos"). Helper `web-app/src/modules/turnos/lib/busqueda.tsx` (`useBusquedaDiferida` borrador/aplicado/version + `AvisoBuscar`); `useTurnos`/`usePrestaciones` aceptan `{ enabled, version }`. De paso el filtro Estado y los chips suman `llamado`/`ausente` (F3). **(2) Colero — decisiones de César:** chime **activado con un botón** en `/pantalla/:token` ("Activar sonido" destraba el `AudioContext`; cada llamado nuevo dispara un din-don sintetizado) y **prefijo alfanumérico de 3 caracteres + número** (`ODO-001`): validador en el schema de espacios (`normalizar_prefijo_colero`, 422 si no son 3), campo "Prefijo colero" en Agenda → Config → Espacios con preview, y el número destacado de la pantalla escala por largo (con prefijo se partía en dos líneas). **Prefijos cargados en prod** (CON Consultorio Municipal · MES Mesa de Atencion Municipal · ODO Sala Municipal de Odontologia · SUM Salon de Usos Multiples · AUD Auditorio Municipal — cambiables desde el form) y en local (ATC/MES/ODO/SUM). **(3) F4:** la Guardia va en Secretaría de Salud bajo subárea "Emergencias" (registrado en el plan; la subárea la crea la mig 106). **(4) Turnos demo en prod** para el QA del colero (Sala Municipal de Odontologia; ver el pendiente de QA). Verificado: typecheck, smoke local del prefijo 7/7 (422 con 4 chars, ` odo `→`ODO`, llamar → `ATC-003`, pantalla pública lo lista), navegación de las 5 tabs + Mesa + form de espacio + pantalla en `5173`. Diagnóstico del "Failed to fetch" de la captura de César: era local con uvicorn caído (la ubicación "Sala de Atencion Central" no existe en prod), no un problema de prod.

- **2026-09-06 — ATENCIÓN F3: CICLO DE LLAMADO + PANTALLA DEL COLERO (mig 105) — EN PROD.** El vecino ya tiene número y la sala tiene su TV. **FSM**: `reservado → llamado → cumplido | ausente` (cumplir directo desde reservado sigue andando: mesa sin colero). **Mig 105** en local Y prod: estados nuevos, `turnos.numero_diario`, tabla `turno_llamado` (log append-only: re-llamar = fila nueva), `espacios_agenda.token_pantalla` + `prefijo_colero`. **Backend**: `PATCH /turnos/{id}/llamar` (número por advisory lock `colero:{ubicacion}:{fecha}`, re-llamar = mismo endpoint, CAS de estado) y `/ausente`; `GET /turnos/publico/pantalla/{token}` **sin auth**, rate limit `pantalla:`. **Frontend**: panel "Atención del día" en la Mesa (número + Llamar/Re-llamar/Atendido/Ausente + campo Puesto recordado + "Abrir pantalla"/"Copiar link" solo nivel ≤2) y `/pantalla/:token` fullscreen, polling 5 s, que **no se borra si se cae la red** (avisa recién tras 3 fallos). **Privacidad**: la pantalla proyecta EN SQL solo número + "Nombre I." + puesto — apellido completo, DNI, prestación e id de turno nunca salen del backend. **Decisiones que cerraban pendientes del plan**: el número se asigna al PRIMER llamado (un turno que nunca se llama no consume número), formato correlativo de 3 dígitos con prefijo opcional por ubicación, y **`ausente` NO libera el slot** (solo cancelar lo hace). Queda pendiente el chime en la pantalla (autoplay en TVs exige interacción previa). Smokes 18/19 + 12/12 + verificación navegando mesa y pantalla. **Siguiente: F4 (guardia + derivación desde Emergencias, mig 106).**

- **2026-09-06 — AGRUPACIÓN DE TRÁMITES POR GESTIÓN (mig 104) + ciclo de vida ACTIVADO + color de marca verde — EN PROD.** Ordenamiento fijado por César para el smoke de la demo: *"todo lo relacionado con trámites tiene que estar asociado a algo así como la Secretaría de Gobierno, esa sería el área, después la subárea"*. Tres decisiones suyas: (a) las 7 subáreas del circuito cuelgan TODAS de Gobierno — el expediente es de Gobierno aunque el tema sea de otra secretaría; (b) el área 1 pasó de `Gobierno` a **`Secretaría de Gobierno`** (consistencia con las otras cinco); (c) la relación tipo→subárea se hizo **explícita** (`tipo_tramite.id_subarea`, mig 104 en local Y prod) en vez de quedar implícita en el circuito. **El área NO se guarda: se deriva por JOIN subarea→area.** Backend: los 4 schemas + POST + PUT (0 = desasignar) + los DOS SELECT de salida; el listado admin ordena área → subárea → tipo. Frontend: columna "Gestión" en el catálogo y selector agrupado por área en alta y edición de tipo (consume `/agenda/catalogos/subareas` por URL directa, sin dependencia cruzada). **`seed_tramites.py` corregido — era el fondo del pedido:** repartía las subáreas entre Gobierno y Servicios Públicos, tenía un bug de precedencia OR/AND, y `upsert_subarea` nunca corregía el área de una subárea preexistente → el ordenamiento salía distinto según el estado previo de la base. Ahora es determinístico (verificado corriendo el seed: 9 tipos, 1 área, 0 sin gestión). Corrige además un desvío real: `aviso-obra` iba a Habilitaciones Comerciales en vez de Obras Particulares. **Trampa §24 cazada aplicando local primero:** `id_area = 1` hardcodeado mandó las subáreas a *Salud* en local (en local el área de gobierno es la 15) → se resuelve por nombre. **Numeración: la 104 la tomó esto, así que el colero de Atención pasa a la 105 y la guardia a la 106** (`PLAN_MODULO_ATENCION.md` renumerado). Migración, backfill y reglas en `HISTORIAL_MIGRACIONES.md` + skill `modulo-tramites`. Backups en prod: `_backup_ciclo_vida_2026_09_06` y `_backup_agrupacion_tramites_2026_09_06`.

- **2026-09-01 — 5ª TANDA BI (2 vueltas) + QA DEL BI CERRADO POR CÉSAR — EN PROD** (`6e035dc` + `ca15a37` + dists CI `56a5b5b`/`e34585a`; Railway confirmado por marker OpenAPI; verificado navegando en local Y en el iframe de prod; **OK final de César**). QA de César sobre prod en 2 mensajes → 2 commits: (1ª vuelta) **tablas del módulo sin scroll horizontal** (numéricas a ancho mínimo `width:1%`+nowrap, columna de nombres envuelve) · **Operativo con los defaults y el tratamiento del Ejecutivo**: abre con año en curso + mes anterior + "Todas las áreas" (admin; supervisor su área), "Limpiar" restaura el default completo, período en letras por sección, y `/bi/comparativo` compara contra el **período CONTIGUO anterior** (regla espejada de `_rango_anterior`; key `anio_anterior` queda como nombre legacy; card "Período anterior" con "sin datos en X" y color por valoración) · fix de bug preexistente: `/bi/ejecutivo/top-tipos` pasaba `ant=None` y la columna % Var de los tops mostraba siempre "—". (2ª vuelta) columnas de texto con `overflowWrap:'anywhere'` (único valor que reduce el min-content — con `break-word` la tabla quedaba RECORTADA a la derecha) + margen contra el scrollbar · pastillas de donas **clampeadas al área del chart** + floats a 1 decimal (imprimía "28.400000000000002") · **cover que tapa la franja sobre la barra sticky** (el scroller embebido `main.embeddedContent` con padding-top 16 pega la sticky al borde del contenido y ahí se veía pasar la página). Reglas nuevas en la skill `modulo-bi` (sección "5ª tanda"). **La regla del período anterior ahora vive en TRES puntas espejadas**: `_rango_anterior` ↔ `bi_comparativo` ↔ `periodo.ts`.

> Detalle histórico completo (incl. julio 2026: auditoría de seguridad 48/48, migs 95-98, PWA de Roy a prod, Usuarios en React): `HISTORIAL_MIGRACIONES.md` + tabla "Histórico breve" de la bitácora.

---

## 🔗 Mapa de fuentes de verdad (para no buscar a ciegas)

| Querés saber… | Mirá… |
|---|---|
| Reglas de cómo se trabaja (mandatorias, transversales) | [`CLAUDE.md`](CLAUDE.md) |
| Detalle de UN módulo (tablas/endpoints/FSM/quirks) | la skill `.claude/skills/modulo-<nombre>/` — carga sola al tocar ese módulo (ver `PLAN_REFACTOR_SKILLS.md`) |
| Estado de avance / pendientes HOY (backoffice) | **este archivo** |
| Estado de avance / pendientes HOY (PWA) | `zaris-vecinos/ESTADO.md` |
| Roadmap detallado de la App Vecinos | [`PLAN_APP_VECINOS.md`](PLAN_APP_VECINOS.md) |
| Roadmap detallado de Emergencias | [`PLAN_MODULO_EMERGENCIAS.md`](PLAN_MODULO_EMERGENCIAS.md) |
| Roadmap de Atención por ubicación (Turnos, colero, Guardia, historia clínica, BI) + decisiones por fase | [`PLAN_MODULO_ATENCION.md`](PLAN_MODULO_ATENCION.md) |
| Qué migración hace qué / cuándo se aplicó | [`HISTORIAL_MIGRACIONES.md`](HISTORIAL_MIGRACIONES.md) |
| Contrato de la API pública que consume la PWA | [`docs/contrato_api_publica.md`](docs/contrato_api_publica.md) |
| Cómo arrancar local / colaborar | [`ONBOARDING.md`](ONBOARDING.md) · [`CONTRIBUTING.md`](CONTRIBUTING.md) |
| Quirks/patrones/decisiones (foto de las memorias) | [`docs/memoria-proyecto/`](docs/memoria-proyecto/) |
