# ESTADO — zaris-zge (backoffice + backend)

> **Hilo conductor común Cesar ↔ Roy.** Este archivo es la **fuente de verdad compartida y versionada** del estado de avance. Es lo PRIMERO que se lee al retomar y lo que se ACTUALIZA al cerrar cada sesión (regla CLAUDE.md §45). Es un documento **vivo y corto** — foto del estado actual, NO bitácora histórica (esa vive en [`HISTORIAL_MIGRACIONES.md`](HISTORIAL_MIGRACIONES.md) y, para Cesar, en su memoria privada de Claude Code).
>
> **Reglas de mantenimiento:**
> - Actualizar al cerrar sesión (la skill `/cierre-sesion` lo hace antes de tocar la memoria privada).
> - "En curso" y "Pendientes" reflejan lo REAL — si terminás algo, movelo a "Hecho reciente" (y podá lo viejo de ahí: máx ~10 líneas).
> - Convertir fechas relativas a absolutas.
> - Verificar contra git/prod antes de declarar algo hecho (`feedback_verificar_siempre_antes_de_opinar`).
> - La PWA App Vecinos tiene su **propio** `ESTADO.md` en el repo `zaris-vecinos`. Acá va solo backoffice/backend.

**Última actualización:** 2026-08-30 (Cesar, 2ª sesión del día) · rama `main` = `78562c8` (+ dist CI `894a266`) — retomada del proyecto tras 6 semanas. Tres entregas EN PROD el mismo día: (1) quick wins de datos/doc; (2) **backend para la PWA** (`47ebbe1`+`51ca34f`, migs 99-100): perfil, bandeja de avisos, reverse geocoding — **ver AVISO PARA ROY #1**; (3) **ciclo de vida del trámite hacia el vecino** (`78562c8`, migs 101+101b): resultado automático al terminar, aviso PWA + encuesta CSAT de trámites, SLA por tipo, flag `espera_iniciador` y timer de desistimiento — **la configuración del catálogo y la prueba del cronograma quedan para cuando César lo pruebe (ver Pendientes)**; (4) **foto de perfil del vecino** (`2af84c3`, mig 102, esquema de Roy aprobado por César): `POST/DELETE /publico/perfil/foto` + `foto_url` firmada en `GET /perfil`, smoke 34/34 local y prod — **ver AVISO PARA ROY #1, punto 4**; (5) **BI Operativo reestructurado en UNA página** (`ad7aa5d` + dist CI): filtros globales con el área de servicio como selector principal y por defecto, índice fijo, exportación por sección en vez de tablas, toggle Estado/Tipo, mapa con semáforo — deploy verificado en prod (endpoints), **falta que César lo mire en prod** (el navegador integrado no pudo verificar scroll/sticky). Quedan los pendientes de definición (São Paulo, app de funcionarios, **BI Ejecutivo — siguiente paso acordado**, IT-01/08/09). Email editable del vecino: César decidió dejarlo como está (NO editable desde la app). Estado vigente: **AUDITORÍA 2026-07 CERRADA AL 100%**: el último residuo (bypass nivel-2 de Trámites en operar/quien_puede/visados) quedó scopeado con verificación adversarial multi-agente previa + smokes 17/17 y 19/19 (anti-regresión) — ver Hecho reciente. QA visual post-2026-07-18 confirmado OK por César y `smoke_emergencias.ps1` re-corrido 64/64. Queda 1 seguimiento de DATOS demo (subárea Espacios Verdes sin agentes, ver Pendientes).

---

## 🔵 En curso

_(nada en curso ahora mismo — backoffice estable, todo lo abierto es de la PWA, ver su ESTADO.md)_

> ### 📣 AVISO PARA ROY (PWA `zaris-vecinos`) — 3 ENDPOINTS NUEVOS que la PWA ya puede consumir (2026-08-30, EN PROD)
> Cierran tres `TODO(backend)` que dejaste en el código de la PWA. Contrato completo en [`docs/contrato_api_publica.md`](docs/contrato_api_publica.md) §4 / §5 / §10 / §11; todos con JWT scope `publico`. **Nada de lo existente cambió de contrato** (solo se sumó `avisos.no_leidos` a `/mi-resumen`).
> 1. **`GET /api/v1/publico/perfil`** → ficha completa (`telefono, calle, altura, localidad, provincia, latitud, longitud, fecha_nac, nacionalidad, cuil, email_verificado, fecha_alta…`) para `DatosPersonales.tsx` (tu interfaz `PerfilExtendido` ya tiene los mismos nombres). **`cuil` viene `null` + `cuil_es_placeholder=true`** cuando es el placeholder del alta por agente — podés borrar el chequeo `20+DNI+9` del front. **`PUT /publico/perfil`** = edición parcial de contacto/domicilio (`telefono` → 10 dígitos sin 0, se guarda digit-only; `calle/altura/localidad/provincia/latitud/longitud`). Email e identidad NO se editan desde la app (decisión pendiente de César).
> 2. **`GET /api/v1/publico/avisos`** → `{avisos, no_leidos, total}` para `AlertasPage.tsx` (hoy estado vacío). Cada aviso trae `url` relativa (`/reclamos/123`, `/emergencias`) para navegar al tocarlo. `PATCH /avisos/{id}/leer` y `POST /avisos/leer-todos`. El badge de la campana sale de `GET /portal/mi-resumen` → `avisos.no_leidos` (sin request extra). Los avisos los genera el backend en cada cambio de estado de reclamo/reporte (los mismos que disparan el push) — quedan aunque el vecino no tenga push activo.
> 3. **`GET /api/v1/publico/reclamos/geo/reverse?lat=&lon=`** → reemplaza la llamada directa a Nominatim de `src/lib/geo.ts::direccionDesdeCoords`. Devuelve siempre 200: usar `direccion` (corta, "Calle Altura, Localidad"); si `encontrado=false` caer a `direccionGps()` como hoy.
> 4. **Foto de perfil (tu propuesta del 2026-07-06, aprobada por César y EN PROD desde 2026-08-30, mig 102):** `POST /api/v1/publico/perfil/foto` (multipart, campo `archivo`, **solo JPEG**, 1 KB..512 KB, ≥100×100 px — la reducción a 256 px que ya hace `src/lib/perfil.ts` alcanza) y `DELETE /publico/perfil/foto`. `GET /publico/perfil` trae `foto_url` (URL **firmada, TTL 1 h** — refrescar el perfil en vez de cachear la URL) y `foto_actualizada_en`. Reemplaza el guardado en `localStorage` (`zaris_vecino_foto`): la foto ahora sigue al vecino entre dispositivos. Contrato §10.
>
> ### 📣 AVISO PARA ROY (PWA `zaris-vecinos`) — 2 CAMBIOS DE CONTRATO en alta de empresa (2026-07-15)
> **1) `POST /api/v1/publico/alta/empresa` (el que usa la PWA) — se cerró un IDOR.** Era anónimo y tomaba `id_ciudadano` del body → cualquiera podía vincular una empresa a un vecino ajeno. **Ahora exige el vecino logueado** y el representante sale del JWT, no del body.
> - **La PWA DEBE mandar `Authorization: Bearer <token scope publico>`** al llamar a `/alta/empresa`. Sin token → **401**.
> - **`id_ciudadano` ya NO va en el body** (se ignora; el backend usa el ciudadano del token). Quitalo del payload.
> - El resto del contrato (`municipio_slug`, `cuit`, `razon_social`, `id_actividad`, domicilio, contacto…) queda **igual**.
> - Ya estaba previsto por diseño (2026-06-09: "la empresa la carga el vecino logueado desde el portal"), así que si tu request ya iba autenticado, solo sacá `id_ciudadano`. Si iba anónimo, autenticalo.
>
> **2) Regla de negocio reforzada: toda empresa nace con un vecino.** Esto NO afecta a la PWA (el vecino logueado ES el representante), pero por si tocás el alta de empresa del backoffice: `POST /api/v1/buc/empresas` (agente) **ahora exige `id_ciudadano` + `id_tipo_representacion`** y crea empresa + vínculo en una transacción. Sin representante → **422**. El endpoint `POST /buc/ciudadano-empresa` sigue existiendo pero ya no hace falta llamarlo tras el alta (el vínculo se crea solo).

---

## 🟠 Pendientes (backoffice / backend)

> El grueso del trabajo abierto del proyecto hoy está en la **PWA** (ver `zaris-vecinos/ESTADO.md` → pendientes del traspaso 2026-07-16; la Etapa G ya está CERRADA). Acá quedan solo cosas de backoffice/infra.

- **🟢 Fase 3 roles — EN PROD (2026-07-02).** El push + rebuild dist ya se hicieron (commits `059a25f` feat roles + `0b6b5b8` dist, ancestros de `origin/main` — verificado 2026-07-17); solo queda (d) revisar scoping de Turnos/Trámites. Modelo de niveles nuevo (mig 92, YA aplicada en local Y prod): 1=Admin · 2=Supervisor (scopeado subárea) · 3=**Atención** (ve todo, ex Operador) · 4=**Gestión** (NUEVO — agente de OT, listados scopeados a su subárea) · 5=Consultor (los 41 ex-nivel-4 ya renumerados en prod). Ver CLAUDE.md §3. Hecho: scoping bandeja OT + guards de asignación + picker de agentes + listado/stats de reclamos + búsqueda por nro/rango/fechas en la mesa + "Avisar al supervisor" (acción sugerida + comentario, fallback a admins, anti-spam 30 min, notif in-app + mail) + fixes (POST /reclamos sin guard de nivel ni tipo obligatorio). **Pendiente:** (a) ~~`git push` backend + rebuild dist~~ **HECHO 2026-07-02** (commits `059a25f` + `0b6b5b8`); (b) ~~reclasificar usuarios~~ **HECHO 2026-07-02**: subárea "Call Center" creada (bajo Gobierno); quedan en Atención (3) SOLO los 2 con cargo "Operador de Call Center" (elizabethcarpio, mariaacosta — movidos a Call Center) + `operadorcom` (excepción: Emergencias/COM exige nivel ≤ 3, §44); los otros 31 pasaron a Gestión (4). Backups: `_backup_usuarios_niveles_2026_07_02` + `_backup_agentes_subarea_2026_07_02`. **2026-07-02 (2ª pasada, aprobada por Cesar):** las 10 jefaturas (Director/Subdirector/Coordinador/Subsecretaria) promovidas a Supervisor (2). Prod queda: 3 admin · 22 sup · 3 atención · 21 gestión · 41 consultor. Demo congelada REC-58/59 switcheada a involucrados coherentes: OT-2026-000011 → agente `chiarauncal` (n4, Señalamiento Vial, pass testing estándar), supervisor `josewaisbord` (n2, cargo nuevo 'Supervisor Operativo', movido a Señalamiento Vial); `lourdesacosta` (n2, Supervisor Operativo) movida a Alumbrado Público para poder cerrar REC-58 (marisaiturriaga volvió a Luminaria-interno). Guion en `credenciales-testing/guion_gestion_reclamos.html` actualizado (fila agente B + continuación); (c) verificar en prod post-deploy; (d) ~~Gestión de Turnos/Trámites: revisar scoping~~ **CERRADO 2026-07-18** (`ff24619`): las mutaciones de Turnos scopean igual que la lectura y Trámites sumó gate anti-Consultor + guard de comentar + nivel 2 a su colectivo en tomar/liberar/resultado (resto del bypass nivel-2 diferido, ver el ítem de auditoría abajo). **Fase 3 roles COMPLETA.**
- **🟠 Config/UX — pendientes de la reunión 06/07/2026 (decisiones de César, NO resueltos en la sesión del 15/07).** Los ajustes acotados (IT-02..06) ya están en prod (ver Hecho reciente). Quedan abiertos, requieren definición tuya:
  - **IT-01 Multi-tenant + super administrador + selector de municipio** — es un PROYECTO de arquitectura, no una tarea. Hoy es mono-tenant (`id_municipio` NULL, §38/§43). La "identidad" del municipio está repartida entre la tabla `municipios` (nombre/provincia/país/`codigo_corto`) y `configuracion_general` (nombre visible del topbar/logo/color) — consolidarla en una ficha única es la base de IT-01. Diseñar por fases con César antes de tocar código.
  - **IT-08 Botón SOS (App Vecinos)** — aprobado a nivel diseño; falta que César defina QUÉ acción dispara antes de conectarlo a backend.
  - **IT-07 Accesibilidad app móvil** (tipografía grande, botones auto-explicados) — toca la PWA `zaris-vecinos` (otro repo → su `ESTADO.md`).
  - **IT-09 Módulo de Obras** — backlog, sin alcance definido.
  - Informe original: PDF adjunto en la tarea "Informe Bug" de la lista **CONFIGURACION y UX** de ClickUp (queda `en curso` a propósito por estos pendientes).
- **🟡 BI Ejecutivo** — pendiente de PRODUCTO, **es el siguiente paso acordado con César (2026-08-30)**: antes quiso revisar el Operativo (hecho, ver Hecho reciente). El usuario define las visualizaciones; NO inventarlas. Tarjeta "Ejecutivo" en `DatosLanding.tsx` es placeholder. (CLAUDE.md §43.)
- **🟡 QA visual del BI Operativo en prod (César) — 2ª vuelta tras los 5 ajustes del 2026-08-30:** confirmar (a) que al hacer click en el índice la sección queda debajo de la barra y el strip "Sección · …" cambia al scrollear, (b) que la fila de KPIs entra en una línea en tu pantalla, (c) las tildes de meses + "Seleccionar año completo", (d) el panel "Filtrado de análisis" en naranja. Lo verifiqué en el DOM en local (todo OK) pero el navegador integrado en modo degradado tiene viewport de alto 0 y no permite validar scroll/sticky.
- ~~🟡 QA visual post-sesión 2026-07-18~~ **CERRADO 2026-07-19**: César navegó y dio el OK visual (tablero de pánico, pastillas BI en dark, labels "Entradas" en Encuestas). El sub-ítem (d) re-corrida de `smoke_emergencias.ps1` se ejecutó contra local el 2026-07-19 (ver Hecho reciente).
- **🟡 Barrido visual modo oscuro** de módulos React restantes — solo Dashboard y Guías verificados en dark; queda el repaso VISUAL navegando (QA). Los 6 literales claros conocidos quedaron resueltos el 2026-07-18 (`6c5e040`): las 4 pastillas de gráficos BI pasaron a tokens invertibles `--fg-1`/`--surface-100`, y los 2 casos de texto sobre color fijo (Recepción de Emergencias, botón primario de Identidad) quedaron documentados como intencionales (caso avatar §13). Vanilla (admin_tablas) tiene el bootstrap pero sin verificación visual dark.
- **🟠 Latencia por región — migrar API + DB a São Paulo (`sa-east-1`).** Verificado 2026-07-15: el proyecto Supabase `ZARIS GESTION ESTADO` (`lshfwsscvfsklrmbvkwl`) está en **`us-east-1`** (Virginia, hemisferio norte); la org ya tiene otro proyecto en `sa-east-1` (`zaris-news-bot`), así que la región destino está probada. Parte del piso de latencia documentado (~2-3s Railway↔Supabase) es geografía. **Falta verificar la región de Railway** (no la vimos aún): la latencia percibida = Railway↔Supabase + cliente↔Railway; mudar solo la DB puede no ganar nada si Railway sigue en US — el objetivo es que **backend Y DB** queden en `sa-east-1`. Mover un proyecto Supabase de región NO es un toggle: implica proyecto nuevo en `sa-east-1` + migrar datos + repuntar connection strings/env vars → **sesión dedicada con backup y ventana**, no un cambio al pasar. Pedido de César 2026-07-15.
- ~~🟡 Encuestas para TRÁMITES — sin soporte~~ **HECHO 2026-08-30** (mig 101+101b, ver Hecho reciente): se disparan cuando el trámite termina con resultado aprobado/rechazado (decisión de César).
- **🟠 CONFIGURAR EL CATÁLOGO DE TRÁMITES para que el ciclo de vida hacia el vecino ACTÚE (César, desde el builder — sin código).** El motor está en prod pero arranca neutro: **0 estados marcados "Espera al iniciador" y 0 tipos con SLA** → el timer de desistimiento no hace nada y el resultado automático solo aplica a transiciones finales etiquetadas `aprobar`/`rechazar`. Para activarlo por tipo: (a) en cada estado donde el expediente espera documentación/respuesta del vecino (ej. `subsanacion`, `pago_pendiente`), tildar **"Espera al iniciador"** en el modal del estado (se puede aunque la versión esté publicada); (b) cargar el **SLA (días)** del tipo en Editar tipo (0 = usa `tramite_sla_dias_default`=30 de Config → Sistema); (c) revisar que la transición que lleva al estado final tenga `tipo_accion` **aprobar/rechazar** (hoy los seeds usan `avanzar`, ej. "Archivar" de poda-árbol → el resultado queda pendiente para marcarlo a mano). Plazos globales en Config → Sistema: aviso 1 +30 días, aviso 2 +60, último aviso 72 h antes, desistido a +90 (`tramite_desistimiento_*`), switch `tramite_desistimiento_activo`.
- ~~🟡 Branding App Vecinos: cargar `municipio_color_primary` en prod~~ **HECHO 2026-08-30**: `#1e5aa8` (azul institucional, elegido como valor inicial para que la PWA deje de caer al verde default — cuando César defina el color de marca real se cambia desde **Config → Identidad**). Verificado en `GET /publico/identidad-municipio` de prod. El acento sigue `#0fd7ff`.
- ~~🟢 Doc: el prefijo `ALERTA DE PANICO` no figura en `docs/contrato_api_publica.md`~~ **HECHO 2026-08-30**: nota bajo la tabla de §8 Emergencias del contrato (prefijo, normalización strip/mayúsculas/sin tildes, efectos en backoffice, `GET /eventos` no expone el flag; el body no puede setear `es_panico`, por diseño).
- **🟡 Vercel: deploys de Roy quedan BLOCKED (autor fuera del team).** Mitigado: `git merge --no-ff` al integrar sus ramas (regla en CLAUDE.md de `zaris-vecinos`) o commit vacío de re-trigger. Fix de raíz (decisión de César): sumarlo al team en el dashboard (asiento Pro facturado). **Verificado 2026-08-30 (Vercel MCP):** el team `ZARIS TEAM WEB` (`cesar-zarinis-projects`) está en plan **Hobby**, que no admite miembros → sumar a Roy implica pasar el team a Pro (facturación por asiento; confirmar precio vigente en el dashboard). Es un cambio de facturación que solo César puede hacer; mientras tanto sigue el `merge --no-ff`.
- **🟢 Higiene (requiere al usuario, no se puede desde Claude Code):** rotar `DISPATCHER_TOKEN` (visible en historial git): valor nuevo → Railway env var → GitHub secret (lo usan 3 crons: encuestas, trámites, integridad) → borrar el secret sobrante `ZARIS_DISPATCHER_TOKEN`.
- **🟢 Credenciales de prod expuestas — mitigado y LIMPIADO (auditoría 2026-07-18).** La password universal `123456` de 12 cuentas (incl. admins `cesar@` id6 y `roymanos@` id3) estaba publicada en el repo público (guías QA `docs/qa_*.html`, skills, smokes). Se forzó `debe_cambiar_password=true` en las **12** (backup `_backup_debe_cambiar_pwd_2026_07_18` en prod) **y las credenciales se SACARON de todos los artefactos trackeados** (`8efbe1c`): smokes/seeds prod-capaces leen `ZARIS_QA_PASS`/`ZARIS_SEED_*` de env (default dev solo local), guías QA y docs referencian `credenciales-testing/`. **Nota para Roy:** si tu login te pide cambiar la clave, es por esto — normal; y para correr smokes contra prod ahora seteás `ZARIS_QA_PASS`. **Pendiente vivo (decisión de César):** evaluar purga del historial git (las claves viejas siguen en commits pasados; con el cambio forzado ya están muertas).
- ~~🟠 Auditoría de seguridad — último residuo (bypass nivel-2 de Trámites)~~ **CERRADO 2026-07-19** (ver Hecho reciente) — **la auditoría 2026-07 queda 100% completa** (48/48 hallazgos resueltos, refutados o aceptados con decisión). Derivado de DATOS demo — **RESUELTO 2026-08-30**: el agente 91 Juan Pestto (usuario `juanpesto` id 2, nivel 4 Gestión, sin OTs/trámites/aprobaciones/turnos/equipos) pasó de Censos (63) a **Espacios Verdes (74)** (`agentes.id_subarea` y `usuarios.id_subarea` a 74; Censos queda con 2 agentes activos). Con eso los 3 `poda-arbol` vivos ya puede avanzarlos ese usuario, no solo un admin. Revertir = volver ambos campos a 63. Actualizar la planilla de `credenciales-testing/` (lo lista como "Operador" sin subárea). Las demás subáreas usadas por circuitos/visados están pobladas (verificado en prod: 68=1, 72=1, 73=1, 70=2, 1=3, 69=3, 71=4 agentes). Residuos conscientes documentados en la skill `modulo-tramites` (catálogo admin sigue nivel ≤2, lectura sin scope, re-resolver visados).

---

## ✅ Hecho reciente (últimas sesiones)

- **2026-08-30 — BI Operativo en UNA página — EN PROD** (`ad7aa5d` + dist de CI; deploy Railway confirmado por marker `/bi/mi-area`; verificación read-only en prod con `administrativo` n2: área por defecto 22 "Servicios Públicos" desde su agente, filtros estado/canal, histograma por tipo, export del universo con `X-Total-Count`, `dias_demora` en el geo). Decisiones de César tras revisar los 4 tableros Power BI de Vicente López (AskUserQuestion): **una sola página** con índice fijo en vez de 4 tabs · **Subreclamos como sección propia al pie** · **filtros globales** = fechas con atajos de año/mes + área + prioridad + **estado + tipo (buscador) + canal** · **histórico con toggle Estado/Tipo** (top 6 tipos + Otros) · y, pedido en el momento, **el área de servicio como selector principal con una por defecto** ("estas vistas son para cada área"): Supervisor → la de su agente (`GET /bi/mi-area`, regla §3, solo áreas activas), Admin → la última usada o la sugerida, "Todas" solo admin. **Sin tablas de detalle: cada sección exporta los tickets filtrados** (Resumen exporta el universo vía `/bi/reclamos-detalle`). Mapa de pendientes con semáforo de demora. Backend: filtros `estado`/`id_tipo_reclamo`/`canal` aplicados por `_aplicar_extras` en los 28 endpoints + 4 nuevos. Frontend: `OperativoPage` + `sections/*` + `FiltrosGlobales` + `TipoSearch`; las rutas viejas por tab siguen y desplazan a la sección. Verificado navegando en local (área por defecto, chips, buscador, cambio de área, Limpiar, toggle Tipo, export → 200). **Ajustes de César tras verlo en prod (misma tarde):** la barra fija muestra siempre la **sección activa** (scroll-spy) y al saltar desde el índice el título queda debajo de los filtros · **KPIs en UNA sola fila** por sección, siempre con el totalizador de lo filtrado + **promedio mensual del último año** + **mismo período del año anterior** con variación (endpoint nuevo `GET /bi/comparativo`), Respuesta con tiempo de cierre promedio y % SLA · panel **"Filtrado de análisis" en naranja** · **meses como tildes independientes + "Seleccionar año completo"** (filtros `anio`/`meses` en los 29 endpoints). **QA visual en prod pendiente de César** (ver Pendientes). Reglas vivas en la skill `modulo-bi`.
- **2026-08-30 — Foto de perfil del vecino — EN PROD** (`2af84c3`; mig **102** `ciudadanos.foto_path`/`foto_actualizada_en` en local y prod; deploy confirmado por marker OpenAPI; `smoke_publico_perfil_avisos.py` **34/34 local y prod**, con subida real al bucket). Esquema de Roy aprobado por César: bucket privado de adjuntos, path fijo `perfiles/{id_ciudadano}.jpg` con upsert (1 foto, sin huérfanos), solo JPEG 1 KB..512 KB y ≥100×100 px (dimensiones leídas del SOF, sin sumar Pillow a requirements), `foto_url` firmada TTL 1 h en `GET /perfil`, `DELETE` idempotente, rate-limit 5/min. **Para Roy:** punto 4 del aviso en "En curso" (la PWA deja el `localStorage`).
- **2026-08-30 — Ciclo de vida del trámite hacia el vecino — EN PROD** (`78562c8` + dist CI `894a266`; migs **101** DDL y **101b** seed en local y prod; deploy Railway confirmado por marker OpenAPI; smoke `smoke_tramites_ciclo_vida.py` **23/23 local** — solo local porque viaja en el tiempo por SQL; verificación read-only en prod). Decisiones de César (AskUserQuestion): standby = **flag por estado** `espera_iniciador` · cronograma **+30 / +60 / 72 h antes / desistido a +90** (configurable) · desistido = **marca paralela** `resultado='desistido'` + `archivado_motivo='desistimiento'` · **resultado derivado de la transición final** (`aprobar`→aprobado, `rechazar`→rechazado). Entregado: `services/tramites/ciclo_vida.py` (resultado auto en la misma transacción; `al_terminar` post-commit = aviso PWA `tramite_estado` + push + encuesta CSAT de trámites; `procesar_desistimientos` en el cron diario = avisos `tramite_pendiente` bandeja+push+**mail** + movimiento `aviso_iniciador` + notificación interna al colectivo "contactalo", y DESISTIDO automático con movimiento `desistido`) · `tipo_tramite.sla_dias` + `tipo_tramite_estado.espera_iniciador` en el builder (SLA en Editar tipo; flag en el modal de estado vía `PATCH /admin/tramites/estados/{id}/espera-iniciador`, editable en versiones publicadas) · `POST /resultado` acepta `desistido` (manual) · `ResultadoChip` y timeline · encuestas: 4ª rama `id_tramite` + plantilla "Encuesta de satisfacción de trámites" + render de mail · `archivar_inactivos` excluye los estados que esperan al vecino · 6 claves nuevas en Config → Sistema. **Para Roy:** los avisos de trámites llegan a la bandeja con `tipo` `tramite_estado`/`tramite_pendiente` y `url=/alertas` (contrato §11). **Para César:** ver el pendiente "CONFIGURAR EL CATÁLOGO".
- **2026-08-30 — Backend para la PWA: perfil + bandeja de avisos + reverse geocoding — EN PROD** (`47ebbe1` + `51ca34f`; migs **99** `ciudadano_aviso` y **100** `ciudadanos.telefono` VARCHAR(15) aplicadas en local y prod; deploys Railway confirmados por marker OpenAPI; smoke `smoke_publico_perfil_avisos.py` **27/27 local y 27/27 prod**). (1) `publico_perfil.py`: `GET` ficha completa de la BUC con el CUIL placeholder enmascarado, `PUT` parcial solo de contacto/domicilio (allowlist; identidad y email fuera). (2) `publico_avisos.py` + `services/avisos.py`: los hooks post-commit de `push.py` persisten el aviso ANTES del push → cubre reclamos.py + ordenes_trabajo.py + emergencias.py sin tocar routers; `GET` con contadores, `PATCH /{id}/leer`, `POST /leer-todos`, `mi-resumen.avisos.no_leidos`. (3) `GET /publico/reclamos/geo/reverse` con helper `reverse_geocodificar` extraído del `/geo/reverse` del backoffice (compat). **Drift cazado en prod:** `ciudadanos.telefono` era VARCHAR(10) (local 15) → el primer smoke prod dio 500 en el PUT → mig 100 + normalización digit-only con la regla del BUC + `DataError`→422. Datos demo que quedaron en prod: reclamos `id 66` y `67` del Vecino Demo (el 67 pasó a En gestión por `administrativo`, generó el aviso #1). Contrato §4/§5/§10/§11, HISTORIAL migs 99-100, skill `modulo-app-vecinos` y CLAUDE §21 actualizados. **Para Roy:** ver el aviso en "En curso".
- **2026-08-30 — Retomada tras 6 semanas: quick wins de datos/doc (sin código, todo verificado en prod).** (1) `municipio_color_primary` = `#1e5aa8` en `configuracion_general`, verificado en `GET /publico/identidad-municipio` (valor inicial; el definitivo lo define César en Config → Identidad). (2) Agente 91 Juan Pestto (`juanpesto`, n4) movido de Censos (63) a Espacios Verdes (74) para destrabar los trámites `poda-arbol` demo. (3) Prefijo `ALERTA DE PANICO` documentado en `docs/contrato_api_publica.md` §8. (4) Vercel verificado: team en plan Hobby → sumar a Roy requiere Pro (decisión de César, sigue en Pendientes). Región Supabase re-verificada: sigue `us-east-1` (pendiente São Paulo intacto).
- **2026-07-19 — CIERRE TOTAL de la auditoría 2026-07: bypass nivel-2 de Trámites scopeado.** `es_admin` de Trámites bajó de `nivel<=2` a `nivel<=1` (fail-closed): el Supervisor ahora **opera** (transicionar/pase/documentos/relacionar) solo dentro de su colectivo destinatario, **evalúa `quien_puede_jsonb`** como cualquier operador (los gates por subárea del catálogo vuelven a significar algo) y **resuelve visados** solo por membresía del aprobador — el aprobador tercero no-destinatario sigue resolviendo sin tomar. Nuevo: `pertenece_al_colectivo_actual` como fuente ÚNICA de la resolución de colectivo (des-triplicada) con **rescate de bandeja personal** (el supervisor destraba trámites destinados a un agente de SU subárea — caso licencia); `ROL_NIVEL` alineado a mig 92. Método: relevamiento previo de `quien_puede_jsonb`/aprobadores/población de subáreas en prod + workflow de verificación adversarial de 4 lentes (consumers/FSM/subsanación/seguridad — veredicto unánime OK-con-ajustes, los ajustes se incorporaron). Verificado: smoke nuevo 17/17 (operar/quien_puede/visados/bandeja personal/UI) + re-corrida del smoke multi-subárea 19/19 (cero regresión) + marker OpenAPI 403 en `/transicionar` para confirmar deploy. **Para Roy:** el n2 que operaba expedientes de otras áreas ahora recibe 403 con mensaje claro; la UI de transiciones ya se auto-ajusta (flag `disponible` del backend); botones Pase/Relacionar/adjuntar pueden mostrar toast 403 en cross-subárea (deuda UX menor anotada).
- **2026-07-19 — QA visual post-maratón OK (César) + `smoke_emergencias.ps1` 64/64 contra local** (pánico + guards de scope incluidos). De paso se corrigió drift de datos LOCAL: `operadorcom` estaba nivel 4/subárea "Asistencia Directa" → espejado a prod (nivel 3, Policía local id 90).
- **2026-07-18 (sesión maratón, todo EN PROD)** — cierre de TODOS los pendientes accionables del backoffice:
  - **Scope-subárea en mutaciones (cierra la auditoría 2026-07 salvo el residuo de Trámites)** (`ff24619`): guards en OT (tomar/estado + guard espejo del auditor en aprobar/rechazar), Turnos (mutaciones = alcance de lectura), Agenda (10 mutaciones + espacios/disponibilidad/novedades para el supervisor), Trámites (anti-Consultor ×9 + comentar + nivel 2 a su colectivo en tomar/liberar/resultado) y Emergencias (fuente del scope corregida a `agentes.id_subarea`). Smoke multi-subárea 19/19. Reglas por módulo en las skills.
  - **Renovación de credenciales ~1 año** (`2bc6a21` + mig 96): clave vencida → cambio forzado en el próximo login; `password_renovacion_dias` configurable en Config → Sistema (365 default, 0 apaga).
  - **Alerta de pánico de Emergencias** (mig 97): el botón «Seguridad» de la PWA ahora marca `es_panico` server-side, notifica in-app+mail a los operadores COM (fallback admins) y el tablero la muestra primera con card destacada, banner rojo clickeable y sonido. Smoke 7/7 (con mails reales por Resend).
  - **Encuestas CSAT de Entradas** (migs 98+98b): tercera rama del polimorfismo de `encuesta_envio`; acreditar la entrada (por id o QR) dispara la encuesta; plantilla "Encuesta de asistencia a eventos" seedeada. Smoke 4/4.
  - **Guards anti-lockout de usuarios** (`06ec52c`): sin auto-baja y sin desactivar al último admin. **Credenciales fuera de artefactos trackeados** (`8efbe1c`). **Literales de modo oscuro resueltos** (`6c5e040`).
  - **Para Roy (PWA):** la alerta de pánico ya funciona de punta a punta sin cambios en la PWA (el prefijo que ya mandás la activa); los POST de reservar turno/entrada pueden devolver 409 por carrera perdida (mismos mensajes de siempre); smokes contra prod ahora exigen `ZARIS_QA_PASS`.
- **2026-07-18** — **Auditoría 2ª ronda: anti-carrera TOCTOU en turnos y reservas — EN PROD** (mig 95 + commit `ba79f1e`, deploy Railway verificado por marker 409 en `/openapi.json`). Cierra [14][15][37][39]: advisory locks por recurso+fecha / ciudadano+día / DNI (`advisory_lock_tx`), `SELECT ... FOR UPDATE` del evento antes del cupo (`lock_evento_row`, las 3 vías de reserva), 3 índices UNIQUE parciales (slots de turnos agente/espacio + reserva no-cancelada por evento+ciudadano), `IntegrityError`→409 en todos los sitios de escritura, CAS de estado (cancelar∥cumplir∥acreditar ya no se pisan) y guard nuevo: `asistio` por id sobre reserva cancelada → 409. **Regla de producto nueva (César): máximo UNA reserva activa por ciudadano+evento, también en backoffice.** Prod pre-verificado sin datos en conflicto (0 dups; los pares históricos incluían una cancelada → fuera del predicado, sin limpieza). Smoke de concurrencia in-process 13/13 PASS. Reglas vivas en skills `modulo-turnos-entradas` y `modulo-agenda`. **Para Roy (PWA):** los POST de reservar turno/entrada ahora pueden devolver 409 también por carrera perdida (mismos mensajes que los checks previos — si la PWA ya muestra el `detail` del 409, no hay nada que cambiar); el cancelar público de una reserva ya acreditada ahora devuelve 409 en vez de pisarla.
- **2026-07-16** — **Sesión completa Cesar (post-traspaso de Roy):**
  - **(a) PWA de Roy EN PROD**: rama `feature/tenant-theming-noticias-emergencias` mergeada y verificada en `vecinos.zaris.com.ar` (detalle en `zaris-vecinos/ESTADO.md`). Causa raíz de los deploys trabados de Vercel identificada (commit-author fuera del team → BLOCKED) + regla `merge --no-ff` documentada en el CLAUDE.md de la PWA.
  - **(b) Config reorganizado** (`5ad2562`): tab **Identidad unificada** (nombre+logo+texto bienvenida+colores de la App Vecinos con color picker y validación hex también server-side — antes los colores estaban en Sistema) y va ÚLTIMA; Config queda 2 tabs (Sistema · Identidad).
  - **(c) Usuarios MIGRADO A REACT** (`fac7dd4` + merge UX `68b252d`): `modules/usuarios/` — maestro completo (port del vanilla, que se borró) con **permisos por usuario integrados en el detalle** (sub-tab Datos|Permisos, `PermisosPanel`) + tab Catálogo de módulos; ítem "Usuarios" de vuelta en el sidebar (`data-modulo="usuarios"`); `/config/permisos|modulos` y `/usuarios/permisos` redirigen. Verificado navegando local (alta E2E con clave temporal, baja/reactivar, override de permisos guardado y revertido) + bundle confirmado en prod (`index-CCHYY87H.js`).
  - **(d) Verificaciones**: encuestas CSAT funcionando E2E en prod (reclamos+turnos; entradas/trámites SIN soporte → Pendientes); cadena de branding PWA completa (falta cargar el color primario → Pendientes); "pages build and deployment" falló 2 veces por **503 transitorio de la API de GitHub** (Liquid Exception del plugin metadata) → re-run vía REST y deploy OK.
- **2026-07-15** — **Alta de empresa: IDOR cerrado + regla "empresa exige vecino" forzada en las 2 vías — EN PROD.** Dos frentes del mismo tema:
  - **(a) IDOR en la vía pública (App Vecinos) — hallazgo de auditoría [09].** `POST /api/v1/publico/alta/empresa` era anónimo y tomaba `id_ciudadano` del body → cualquiera podía vincular una empresa a un vecino ajeno (IDs secuenciales). **Fix:** exige `get_current_ciudadano` (scope `publico`); el representante sale del token, no del body; se quitó `id_ciudadano` de `AltaEmpresaIn`. Verificado in-process: sin token→401, scope agente→401, vecino logueado con `id_ciudadano:999` en body→201 con vínculo al ciudadano del token. Archivos: `publico_alta.py` + `schemas/publico_alta.py`.
  - **(b) Regla de negocio "toda empresa nace con un vecino" (BUC §2), pedida por César.** Se descubrió que la vía de backoffice `POST /buc/empresas` (agente/call center) creaba la empresa **suelta** (el vínculo era un 2º endpoint separado que el frontend podía omitir). En prod hay **500 empresas huérfanas** pero son un **seed de padrón** (lote único 14/05, todas sin `id_usuario_alta`) → NO se backfillean (regla `no_backfill_sobre_datos_sinteticos`). **Fix:** `POST /buc/empresas` ahora exige `id_ciudadano` + `id_tipo_representacion` y crea empresa + vínculo `ciudadano_empresa` en **una transacción** (sin representante → 422). Frontend módulo Empresas (`FormView`) suma selector de vecino (`CiudadanoSearch`) + tipo de representación obligatorios en el alta; el flujo desde Ciudadano (`EmpresaVinculadaPanel`) se simplificó a 1 request. Verificado: smoke backend (422/201+vínculo) + navegación local (alta sin vecino bloquea con error, pick OK, catálogo tipos OK). Archivos: `buc.py`, `schemas/buc.py`, módulos React `empresas/` + `ciudadanos/`.
  - **⚠️ 2 cambios de contrato para la PWA de Roy → ver el aviso destacado en "En curso".**
- **2026-07-15** — **Testing traspapelado de Roy (ClickUp) revisado + resuelto lo vivo — TODO EN PROD.** Roy había dejado ~1 mes atrás informes de testing como PDFs adjuntos en las tareas de ClickUp (workspace ZARIS ZGE; conectado por MCP `@taazkareem/clickup-mcp-server`, token en `~/.claude.json` user scope). Se revisaron los 7 bugs reproducibles verificando cada uno contra código + prod (no a ciegas):
  - **VIVOS, corregidos y verificados en prod:** **REC-001** (cancelar reclamo padre dejaba subreclamos huérfanos → guard 422 en `PUT /reclamos/{id}/cancelar` + reparado dato huérfano `REC-2026-000052`; backup `_backup_reclamos_rec001_2026_07_15`) y **EM002** (faltaba filtro DESDE-HASTA en el tablero Emergencias → date pickers + modo histórico vía `useEventos`; el backend ya lo soportaba).
  - **Ya resueltos / no reproducían:** TR-04, TR-05, OT-001, EM003. **EM001** = decisión de diseño (se mantiene). Commits `1b9b0d3` (+ auto-dist `d6c741f`).
  - **Config/UX (informe reunión 06/07):** ajustes acotados **IT-02..06 en prod** — municipio → "Municipalidad de San Andrés"/prefijo `SAN`/`pais` (mig **94** + UPDATE, backup `_backup_municipios_it02_2026_07_15`); form de municipio edita país+prefijo (admin_tablas); se quitó atajo duplicado "Todos los maestros" de Config; **botón "Alta vecino" en el topbar** con enlace + QR descargable (lib `qrcode` bundleada a `frontend/js/qrcode.bundle.js`, IIFE standalone — el shell vanilla no tiene bundler); ícono de notificaciones ya estaba OK. Commit `effd2a7`. **Pendientes IT-01/07/08/09** → ver Pendientes.
  - ClickUp actualizado: comentario con fecha + estado `completado` en Reclamos/OT/Emergencias/Trámites; Config queda `en curso` (tiene pendientes).
- **2026-07-03** — **Auditoría integral de seguridad por módulos — ronda 1 EN PROD.** Se auditaron los 11 clusters de módulos (integridad, workflows, vulnerabilidades) con verificación adversarial. Patrón raíz: endpoints que autenticaban a nivel router pero no autorizaban por `nivel_acceso` en el handler. Pusheado y deployado:
  - `ae90753` — guard `require_admin` (core) aplicado por-endpoint en mutaciones de `usuarios` (buc.py), CRUD de Maestros (admin_tablas.py) y `configuracion_general`; guard auditor/admin en `aprobar_ot`/`rechazar_ot`; login-log admin-or-self; login interno con rate-limit; GET de agentes oculta DNI/CUIL/email/tel a no-admin (sin romper el picker de OT); fail-fast si `SECRET_KEY` es el default público en no-local. Verificado: smoke local 13/13 + deploy confirmado en prod.
  - `677a19d` — rate-limit en públicos que no lo tenían (turnos/agenda `/reservar`, login vecino por IP+DNI, reenviar/recuperar por IP) + `get_real_ip` prefiere `X-Envoy-External-Address` (no-regresivo, anti-spoof). **Verificado en prod**: spoof de `X-Forwarded-For` ya NO evade el rate-limit por IP (Envoy expone la IP real en Railway). Prod tiene `SECRET_KEY` real (probe de JWT forjado → 401).
  - Pendientes de 2ª ronda (anti-carrera + scope-subárea) → ver Pendientes.
- **2026-07-02** — **Dashboard "Resumen de actividad municipal" EN PROD** (`5f5f09a`, verificado navegando prod). Home nueva: título con logo/nombre del municipio + 6 azulejos (emergencias/reclamos/espacios/turnos/entradas/trámites, 102px, tinte por color) + mapa multicapa con markers-icono Lucide y toggles + leyenda al lado del título. Mig 93 (`espacios_agenda.latitud/longitud`, local+prod) + endpoint agregado `GET /api/v1/dashboard/resumen` + form de espacios con bloque §23 (`DireccionGeoField`) + campo `direccion` de Trámites persiste `{texto, lat, lon}` retro-compat (y el detalle ahora monta `DatosTramite` — gap pre-existente). **Para que espacios/trámites aparezcan en el mapa de prod hay que cargarles dirección con pin desde los forms** (los datos viejos no tienen coords).
- **2026-07-02** — **Guion end-to-end de gestión de reclamos + manual con capturas (revisión UX).** Se ejecutó en prod el flujo completo vecino→supervisor→subreclamo cross-área→resolución→cierre+encuesta: caso #1 cerrado (`REC-2026-000056` padre Alumbrado + `REC-2026-000057` subreclamo Señalamiento, encuesta CSAT #15) y caso #2 **congelado a mitad de flujo para reproducir** (`REC-2026-000058` En espera + `REC-2026-000059` con `OT-2026-000011` asignada a agente 18, En gestión — continuar en paso 8 del guion). Quedan como demo (no borrar). Manual HTML autocontenido con 10 capturas reales de prod + tabla de usuarios de testing: fuera del repo, en `Documents\ZARIS\credenciales-testing\` de Cesar (contiene credenciales — pasar a Roy por canal privado). Passwords de testing reseteadas en prod para 4 roles + 2 vecinos + 3 usuarios del guion (dev estándar §1). Hallazgo del scoping de supervisor → Pendientes/Fase 3.
> Detalle histórico completo: `HISTORIAL_MIGRACIONES.md` + tabla "Histórico breve" de la bitácora.

---

## 🔗 Mapa de fuentes de verdad (para no buscar a ciegas)

| Querés saber… | Mirá… |
|---|---|
| Reglas de cómo se trabaja (mandatorias, transversales) | [`CLAUDE.md`](CLAUDE.md) |
| Detalle de UN módulo (tablas/endpoints/FSM/quirks) | la skill `.claude/skills/modulo-<nombre>/` — carga sola al tocar ese módulo (ver `PLAN_REFACTOR_SKILLS.md`) |
| Estado de avance / pendientes HOY (backoffice) | **este archivo** |
| Estado de avance / pendientes HOY (PWA) | `zaris-vecinos/ESTADO.md` |
| Roadmap detallado de la App Vecinos | [`PLAN_APP_VECINOS.md`](PLAN_APP_VECINOS.md) |
| Roadmap detallado de Emergencias | [`PLAN_MODULO_EMERGENCIAS.md`](PLAN_MODULO_EMERGENCIAS.md) |
| Qué migración hace qué / cuándo se aplicó | [`HISTORIAL_MIGRACIONES.md`](HISTORIAL_MIGRACIONES.md) |
| Contrato de la API pública que consume la PWA | [`docs/contrato_api_publica.md`](docs/contrato_api_publica.md) |
| Cómo arrancar local / colaborar | [`ONBOARDING.md`](ONBOARDING.md) · [`CONTRIBUTING.md`](CONTRIBUTING.md) |
| Quirks/patrones/decisiones (foto de las memorias) | [`docs/memoria-proyecto/`](docs/memoria-proyecto/) |
