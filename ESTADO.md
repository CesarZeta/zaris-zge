# ESTADO — zaris-zge (backoffice + backend)

> **Hilo conductor común Cesar ↔ Roy.** Este archivo es la **fuente de verdad compartida y versionada** del estado de avance. Es lo PRIMERO que se lee al retomar y lo que se ACTUALIZA al cerrar cada sesión (regla CLAUDE.md §45). Es un documento **vivo y corto** — foto del estado actual, NO bitácora histórica (esa vive en [`HISTORIAL_MIGRACIONES.md`](HISTORIAL_MIGRACIONES.md) y, para Cesar, en su memoria privada de Claude Code).
>
> **Reglas de mantenimiento:**
> - Actualizar al cerrar sesión (la skill `/cierre-sesion` lo hace antes de tocar la memoria privada).
> - "En curso" y "Pendientes" reflejan lo REAL — si terminás algo, movelo a "Hecho reciente" (y podá lo viejo de ahí: máx ~10 líneas).
> - Convertir fechas relativas a absolutas.
> - Verificar contra git/prod antes de declarar algo hecho (`feedback_verificar_siempre_antes_de_opinar`).
> - La PWA App Vecinos tiene su **propio** `ESTADO.md` en el repo `zaris-vecinos`. Acá va solo backoffice/backend.

**Última actualización:** 2026-09-01 cierre (Cesar — todo EN PROD y **QA visual del BI dado por OK por César**): **5ª tanda BI en dos vueltas** (`64ef616` + `7cfdd16` + dists CI): tablas del módulo sin scroll horizontal (primera columna adapta), Operativo con los defaults y el tratamiento del Ejecutivo (año en curso + mes anterior + "Todas las áreas" admin; comparativa vs período CONTIGUO anterior en `/bi/comparativo`; período en letras por sección), `% Var` real en los tops de Mayores (bug preexistente `ant=None`), pastillas de donas clampeadas + floats formateados, y cover que tapa la franja sobre la barra fija (padding del scroller embebido). Reglas en la skill `modulo-bi` (sección 5ª tanda). Cierre anterior (2026-08-31): **3ª y 4ª tanda de ajustes del Ejecutivo** (`7f1031c` + `1232c88` + `afacdfa`: defaults año en curso + mes anterior + todas las áreas; series mensuales SIEMPRE con tope de 12 meses pero mostrando solo los meses CON datos; barras sat/cierre sin filas ni etiquetas en cero; mapas con puntos mínimos + leyenda en recuadro + **fit real de todos los puntos** — causa raíz: el flag de "ya encuadré" sobrevivía al remontaje del mapa y no había re-fit al cambiar el dataset; donas Niveles + Cierres lado a lado en el Resumen) · **fix Dashboard home** (arrastrar el mapa ya no selecciona el texto de las tarjetas; los huecos panean) · **shell DEV de localhost:5173 clonado del shell vanilla de prod** (topbar/sidebar/statusbar reales — §14) · campanita del shell dev sobre la barra sticky del BI · **incidente cron demo-datos**: GitHub demoró el cron 8 hs (corrió 16:11 UTC) y un dispatch manual pedido antes de saberlo duplicó la semana → triple capa 25-31/8 **limpiada por conexión directa a la DB** (164 filas borradas con backups `_backup_*_dup_2026_08_31` en prod + dump local `backups/zaris_prod_2026-08-31_1930.sql`; agosto quedó en 462 reclamos, ~12-18/día uniforme; regla nueva en la skill `modulo-bi`). Rama `main` = `1dcf2c3` (código `afacdfa` + dist CI). Pendientes de definición: São Paulo, app de funcionarios, IT-01/08/09, áreas fijas. Estado vigente: AUDITORÍA 2026-07 CERRADA AL 100%.

---

## 🔵 En curso

- **🔵 ATENCIÓN POR UBICACIÓN (Turnos reorganizados + colero + guardia + historia clínica + BI por gestión)** — plan aprobado por César 2026-09-01, 6 fases en [`PLAN_MODULO_ATENCION.md`](PLAN_MODULO_ATENCION.md) (decisiones cerradas: ubicación = `espacios_agenda`; gestión = área; **las atenciones derivadas de Emergencias NO van con turno** — registro propio `emergencia_atencion`; historia clínica mínima viable restringida a Salud; colero público tokenizado con "número + nombre e inicial"). **F1 + F2 + F2b HECHAS, verificadas y PUSHEADAS 2026-09-01 (cierre)**: mig 103 aplicada en local Y prod (`tipo_prestacion.id_espacio_ubicacion` + `turnos.id_espacio_ubicacion` copiada al reservar en las 3 vías + backfill + índice) · **2ª decisión de César**: se mantienen turnos por agente Y por espacio, pero la **ubicación es OBLIGATORIA** en toda prestación (422 backend + form; demo backfilleada, 0 activas sin ubicación en local ni prod) · **el módulo Turnos ahora se navega ubicación-primero**: landing de Ubicaciones agrupada por gestión/área con contadores del día (`GET /turnos/ubicaciones`), **Mesa del día** por ubicación (grilla espacio + agentes con disponibilidad efectiva batch + ocupaciones; turnos del agente en OTRA ubicación enmascarados desde el backend; guard subárea nivel 3-4) (`GET /turnos/ubicaciones/{id}/mesa`), contexto persistente (`zaris_turnos_ubicacion`) que scopea Turnos/Agenda/Atendidos vía filtro backend `id_espacio_ubicacion`. **F2b también HECHA el mismo día**: el módulo AGENDA suma la pill "Por ubicación" (primera del toggle) con select agrupado por gestión — la grilla Día/Semana muestra el espacio + sus agentes (backend `id_espacio_ubicacion` en `/agenda/calendario` y `/semana`, helper `_recursos_de_ubicacion`, compat retro verificada; el select consume `GET /turnos/ubicaciones` por URL directa, sin import cross-módulo). Smokes 16/16 + 15/15 + 10/10 PASS + verificación visual completa en localhost:5173. ~~**OJO deploy:** re-correr el backfill de la 103 por los turnos creados en el medio~~ **NO HACE FALTA — verificado en prod 2026-09-06:** el código ya está deployado (`/turnos/ubicaciones` y `/ubicaciones/{id}/mesa` en el OpenAPI de Railway) y no se crearon turnos entre la mig y el deploy (el último turno de prod es del 2026-07-06). Prod hoy: **0 prestaciones activas sin ubicación** y **0 turnos futuros sin ubicación**; los 8 turnos sin ubicación son legacy por agente y ya pasados (el residuo conocido de F2). Siguiente: F3 (llamado + colero + ausente, mig 104).

> ### 📣 AVISO PARA ROY (PWA `zaris-vecinos`) — 3 ENDPOINTS NUEVOS que la PWA ya puede consumir (2026-08-30, EN PROD)
> Cierran tres `TODO(backend)` que dejaste en el código de la PWA. Contrato completo en [`docs/contrato_api_publica.md`](docs/contrato_api_publica.md) §4 / §5 / §10 / §11; todos con JWT scope `publico`. **Nada de lo existente cambió de contrato** (solo se sumó `avisos.no_leidos` a `/mi-resumen`).
> 1. **`GET /api/v1/publico/perfil`** → ficha completa (`telefono, calle, altura, localidad, provincia, latitud, longitud, fecha_nac, nacionalidad, cuil, email_verificado, fecha_alta…`) para `DatosPersonales.tsx` (tu interfaz `PerfilExtendido` ya tiene los mismos nombres). **`cuil` viene `null` + `cuil_es_placeholder=true`** cuando es el placeholder del alta por agente — podés borrar el chequeo `20+DNI+9` del front. **`PUT /publico/perfil`** = edición parcial de contacto/domicilio (`telefono` → 10 dígitos sin 0, se guarda digit-only; `calle/altura/localidad/provincia/latitud/longitud`). Email e identidad NO se editan desde la app (decisión pendiente de César).
> 2. **`GET /api/v1/publico/avisos`** → `{avisos, no_leidos, total}` para `AlertasPage.tsx` (hoy estado vacío). Cada aviso trae `url` relativa (`/reclamos/123`, `/emergencias`) para navegar al tocarlo. `PATCH /avisos/{id}/leer` y `POST /avisos/leer-todos`. El badge de la campana sale de `GET /portal/mi-resumen` → `avisos.no_leidos` (sin request extra). Los avisos los genera el backend en cada cambio de estado de reclamo/reporte (los mismos que disparan el push) — quedan aunque el vecino no tenga push activo.
> 3. **`GET /api/v1/publico/reclamos/geo/reverse?lat=&lon=`** → reemplaza la llamada directa a Nominatim de `src/lib/geo.ts::direccionDesdeCoords`. Devuelve siempre 200: usar `direccion` (corta, "Calle Altura, Localidad"); si `encontrado=false` caer a `direccionGps()` como hoy.
> 4. **Foto de perfil (tu propuesta del 2026-07-06, aprobada por César y EN PROD desde 2026-08-30, mig 102):** `POST /api/v1/publico/perfil/foto` (multipart, campo `archivo`, **solo JPEG**, 1 KB..512 KB, ≥100×100 px — la reducción a 256 px que ya hace `src/lib/perfil.ts` alcanza) y `DELETE /publico/perfil/foto`. `GET /publico/perfil` trae `foto_url` (URL **firmada, TTL 1 h** — refrescar el perfil en vez de cachear la URL) y `foto_actualizada_en`. Reemplaza el guardado en `localStorage` (`zaris_vecino_foto`): la foto ahora sigue al vecino entre dispositivos. Contrato §10.
>
> ### 📣 AVISO PARA ROY (PWA `zaris-vecinos`) — 2 CAMBIOS DE CONTRATO en alta de empresa (2026-07-15)
> **1) `POST /api/v1/publico/alta/empresa` (el que usa la PWA) — se cerró un IDOR.** Era anónimo y tomaba `id_ciudadano` del body → cualquiera podía vincular una empresa a un vecino ajeno. **Ahora exige el vecino logueado** y el representante sale del JWT, no del body.
> - **La PWA DEBE mandar `Authorization: Bearer <token scope publico>`** al llamar a `/alta/empresa`. Sin token → **401**.
> - **`id_ciudadano` ya NO va en el body** (se ignora; el backend usa el ciudadano del token). Quitalo del payload.
> - El resto del contrato (`municipio_slug`, `cuit`, `razon_social`, `id_actividad`, domicilio, contacto…) queda **igual**.
> - Ya estaba previsto por diseño (2026-06-09: "la empresa la carga el vecino logueado desde el portal"), así que si tu request ya iba autenticado, solo sacá `id_ciudadano`. Si iba anónimo, autenticalo.
>
> **2) Regla de negocio reforzada: toda empresa nace con un vecino.** Esto NO afecta a la PWA (el vecino logueado ES el representante), pero por si tocás el alta de empresa del backoffice: `POST /api/v1/buc/empresas` (agente) **ahora exige `id_ciudadano` + `id_tipo_representacion`** y crea empresa + vínculo en una transacción. Sin representante → **422**. El endpoint `POST /buc/ciudadano-empresa` sigue existiendo pero ya no hace falta llamarlo tras el alta (el vínculo se crea solo).

---

## 🟠 Pendientes (backoffice / backend)

> El grueso del trabajo abierto del proyecto hoy está en la **PWA** (ver `zaris-vecinos/ESTADO.md` → pendientes del traspaso 2026-07-16; la Etapa G ya está CERRADA). Acá quedan solo cosas de backoffice/infra.

- **🟢 Fase 3 roles — EN PROD (2026-07-02).** El push + rebuild dist ya se hicieron (commits `059a25f` feat roles + `0b6b5b8` dist, ancestros de `origin/main` — verificado 2026-07-17); solo queda (d) revisar scoping de Turnos/Trámites. Modelo de niveles nuevo (mig 92, YA aplicada en local Y prod): 1=Admin · 2=Supervisor (scopeado subárea) · 3=**Atención** (ve todo, ex Operador) · 4=**Gestión** (NUEVO — agente de OT, listados scopeados a su subárea) · 5=Consultor (los 41 ex-nivel-4 ya renumerados en prod). Ver CLAUDE.md §3. Hecho: scoping bandeja OT + guards de asignación + picker de agentes + listado/stats de reclamos + búsqueda por nro/rango/fechas en la mesa + "Avisar al supervisor" (acción sugerida + comentario, fallback a admins, anti-spam 30 min, notif in-app + mail) + fixes (POST /reclamos sin guard de nivel ni tipo obligatorio). **Pendiente:** (a) ~~`git push` backend + rebuild dist~~ **HECHO 2026-07-02** (commits `059a25f` + `0b6b5b8`); (b) ~~reclasificar usuarios~~ **HECHO 2026-07-02**: subárea "Call Center" creada (bajo Gobierno); quedan en Atención (3) SOLO los 2 con cargo "Operador de Call Center" (elizabethcarpio, mariaacosta — movidos a Call Center) + `operadorcom` (excepción: Emergencias/COM exige nivel ≤ 3, §44); los otros 31 pasaron a Gestión (4). Backups: `_backup_usuarios_niveles_2026_07_02` + `_backup_agentes_subarea_2026_07_02`. **2026-07-02 (2ª pasada, aprobada por Cesar):** las 10 jefaturas (Director/Subdirector/Coordinador/Subsecretaria) promovidas a Supervisor (2). Prod queda: 3 admin · 22 sup · 3 atención · 21 gestión · 41 consultor. Demo congelada REC-58/59 switcheada a involucrados coherentes: OT-2026-000011 → agente `chiarauncal` (n4, Señalamiento Vial, pass testing estándar), supervisor `josewaisbord` (n2, cargo nuevo 'Supervisor Operativo', movido a Señalamiento Vial); `lourdesacosta` (n2, Supervisor Operativo) movida a Alumbrado Público para poder cerrar REC-58 (marisaiturriaga volvió a Luminaria-interno). Guion en `credenciales-testing/guion_gestion_reclamos.html` actualizado (fila agente B + continuación); (c) verificar en prod post-deploy; (d) ~~Gestión de Turnos/Trámites: revisar scoping~~ **CERRADO 2026-07-18** (`ff24619`): las mutaciones de Turnos scopean igual que la lectura y Trámites sumó gate anti-Consultor + guard de comentar + nivel 2 a su colectivo en tomar/liberar/resultado (resto del bypass nivel-2 diferido, ver el ítem de auditoría abajo). **Fase 3 roles COMPLETA.**
- **🟠 Config/UX — pendientes de la reunión 06/07/2026 (decisiones de César, NO resueltos en la sesión del 15/07).** Los ajustes acotados (IT-02..06) ya están en prod (ver Hecho reciente). Quedan abiertos, requieren definición tuya:
  - **IT-01 Multi-tenant + super administrador + selector de municipio** — es un PROYECTO de arquitectura, no una tarea. Hoy es mono-tenant (`id_municipio` NULL, §38/§43). La "identidad" del municipio está repartida entre la tabla `municipios` (nombre/provincia/país/`codigo_corto`) y `configuracion_general` (nombre visible del topbar/logo/color) — consolidarla en una ficha única es la base de IT-01. Diseñar por fases con César antes de tocar código.
  - **IT-08 Botón SOS (App Vecinos)** — aprobado a nivel diseño; falta que César defina QUÉ acción dispara antes de conectarlo a backend.
  - **IT-07 Accesibilidad app móvil** (tipografía grande, botones auto-explicados) — toca la PWA `zaris-vecinos` (otro repo → su `ESTADO.md`).
  - **IT-09 Módulo de Obras** — backlog, sin alcance definido.
  - Informe original: PDF adjunto en la tarea "Informe Bug" de la lista **CONFIGURACION y UX** de ClickUp (queda `en curso` a propósito por estos pendientes).
- ~~🟡 BI Ejecutivo~~ **HECHO 2026-08-30** (`d770f6b`, EN PROD — ver Hecho reciente). ~~Queda su QA visual~~ **QA CERRADO 2026-09-01**: César navegó prod, reportó los hallazgos de la 5ª tanda (2 vueltas, corregidos en el día — ver Hecho reciente 2026-09-01) y dio el **OK final** ("TODO VERIFICADO OK"). El detalle acumulado de las tandas 2ª-5ª vive en la skill `modulo-bi`. Datos demo abr-ago poblados (agosto = 462 tras la limpieza del 2026-08-31).
- **🟠 Áreas fijas por regla de negocio (definición de César, planteado 2026-08-30):** determinadas áreas/secretarías (Seguridad, Salud, Servicios Públicos…) deberían venir **precargadas por default y NO ser removibles** del sistema (renombrables sí), porque atienden funciones básicas de la plataforma directamente vinculadas a módulos (Emergencias→Seguridad, Turnos/prestaciones→Salud, Reclamos→Servicios Públicos). NO requiere cambio de código todavía: es un requerimiento a ESTABLECER en las reglas de negocio y analizar en todo el contexto (seeds, admin_tablas, baja lógica, multi-tenant IT-01). César lo quiere ver después de cerrar los tableros ejecutivos.
- ~~🟡 QA visual del BI Operativo en prod — 3ª vuelta tras la 2ª tanda de ajustes (`d76fffc`)~~ **CERRADO 2026-08-30**: César navegó el Operativo en prod (bundle `index-WC5OyJfy.js` = dist CI `a062bdb`, verificado) y dio el OK a los 7 puntos: índice correcto al click y al scroll, mapa de Pendientes bajo la barra fija, panel de filtros contraíble con resumen, meses sin tilde, total al centro de las donas, doble separación entre visualizaciones y fila de KPIs en una línea.
- ~~🟡 QA visual post-sesión 2026-07-18~~ **CERRADO 2026-07-19**: César navegó y dio el OK visual (tablero de pánico, pastillas BI en dark, labels "Entradas" en Encuestas). El sub-ítem (d) re-corrida de `smoke_emergencias.ps1` se ejecutó contra local el 2026-07-19 (ver Hecho reciente).
- **🟡 Barrido visual modo oscuro** de módulos React restantes — solo Dashboard y Guías verificados en dark; queda el repaso VISUAL navegando (QA). Los 6 literales claros conocidos quedaron resueltos el 2026-07-18 (`6c5e040`): las 4 pastillas de gráficos BI pasaron a tokens invertibles `--fg-1`/`--surface-100`, y los 2 casos de texto sobre color fijo (Recepción de Emergencias, botón primario de Identidad) quedaron documentados como intencionales (caso avatar §13). Vanilla (admin_tablas) tiene el bootstrap pero sin verificación visual dark.
- **🟠 Latencia por región — migrar API + DB a São Paulo (`sa-east-1`).** Verificado 2026-07-15: el proyecto Supabase `ZARIS GESTION ESTADO` (`lshfwsscvfsklrmbvkwl`) está en **`us-east-1`** (Virginia, hemisferio norte); la org ya tiene otro proyecto en `sa-east-1` (`zaris-news-bot`), así que la región destino está probada. Parte del piso de latencia documentado (~2-3s Railway↔Supabase) es geografía. **Falta verificar la región de Railway** (no la vimos aún): la latencia percibida = Railway↔Supabase + cliente↔Railway; mudar solo la DB puede no ganar nada si Railway sigue en US — el objetivo es que **backend Y DB** queden en `sa-east-1`. Mover un proyecto Supabase de región NO es un toggle: implica proyecto nuevo en `sa-east-1` + migrar datos + repuntar connection strings/env vars → **sesión dedicada con backup y ventana**, no un cambio al pasar. Pedido de César 2026-07-15.
- ~~🟡 Encuestas para TRÁMITES — sin soporte~~ **HECHO 2026-08-30** (mig 101+101b, ver Hecho reciente): se disparan cuando el trámite termina con resultado aprobado/rechazado (decisión de César).
- ~~🟠 CONFIGURAR EL CATÁLOGO DE TRÁMITES para que el ciclo de vida hacia el vecino ACTÚE~~ **HECHO 2026-09-06 (lo aplicó Claude Code por DB, en local Y prod — pedido de César: "¿no podés hacerlo vos?").** Antes: 0 estados con `espera_iniciador` y 0 tipos con SLA → el motor estaba encendido (`tramite_desistimiento_activo=true`) pero inerte. Ahora en **prod**: (a) `espera_iniciador=TRUE` en los **6 estados donde la pelota es del vecino** — `subsanacion` de Habilitación comercial (17) y de Permiso Demostración E2E (68/73/82), `pago_pendiente` de Cartel publicitario (40), `subsanar` de Inscripción Profesional (89); los estados de trabajo interno (revisión/evaluación/inspección) quedaron en FALSE a propósito. (b) **SLA por tipo en los 15 tipos activos**: 2, 3 o 4 días (`2 + id_tipo_tramite % 3`, pseudo-aleatorio determinístico alrededor de 3, decisión de César). (c) ⚠️ **Los 2 expedientes vivos que caían bajo el timer** (`HAB-LPL-2026-0001` y `CAR-LPL-2026-0002`, ambos de empresa con **email real de Gmail**, parados hace 122 y 130 días = pasados los 90 del desistimiento) **tenían el reloj a cero** (`fecha_entrada_estado_actual=NOW()`): sin eso, el cron diario les mandaba 4 mails reales y los **desistía automáticamente en ~4 corridas**. Ahora el primer aviso recién a los +30 días. Backup de TODOS los valores previos en la tabla `_backup_ciclo_vida_2026_09_06` de prod. **En local** se aplicó lo mismo (2 estados de espera + 9/9 tipos con SLA) SIN resetear relojes, para poder probar el timer. **Queda abierto (no se tocó, es semántica del circuito):** las transiciones finales de los seeds usan `tipo_accion='avanzar'` en vez de `aprobar`/`rechazar` (solo 6 de todo el catálogo lo tienen), así que el **resultado automático + la encuesta CSAT** todavía no se disparan solos en la mayoría de los tipos. Plazos globales en Config → Sistema: aviso 1 +30 días, aviso 2 +60, último aviso 72 h antes, desistido a +90.
- ~~🟡 Branding App Vecinos: cargar `municipio_color_primary` en prod~~ **HECHO 2026-08-30, cambiado a VERDE el 2026-09-06 por pedido de César**: hoy vale **`#1f8a65`** (el verde del DS, `--color-success`, que además era el default histórico de la PWA antes del branding) — verificado en `GET /publico/identidad-municipio` de prod y aplicado también en local. El azul anterior `#1e5aa8` queda en `_backup_ciclo_vida_2026_09_06`. El acento sigue `#0fd7ff`. Cuando César defina el color de marca definitivo se cambia desde **Config → Identidad** (sin código).
- ~~🟢 Doc: el prefijo `ALERTA DE PANICO` no figura en `docs/contrato_api_publica.md`~~ **HECHO 2026-08-30**: nota bajo la tabla de §8 Emergencias del contrato (prefijo, normalización strip/mayúsculas/sin tildes, efectos en backoffice, `GET /eventos` no expone el flag; el body no puede setear `es_panico`, por diseño).
- **🟡 Vercel: deploys de Roy quedan BLOCKED (autor fuera del team).** Mitigado: `git merge --no-ff` al integrar sus ramas (regla en CLAUDE.md de `zaris-vecinos`) o commit vacío de re-trigger. Fix de raíz (decisión de César): sumarlo al team en el dashboard (asiento Pro facturado). **Verificado 2026-08-30 (Vercel MCP):** el team `ZARIS TEAM WEB` (`cesar-zarinis-projects`) está en plan **Hobby**, que no admite miembros → sumar a Roy implica pasar el team a Pro (facturación por asiento; confirmar precio vigente en el dashboard). Es un cambio de facturación que solo César puede hacer; mientras tanto sigue el `merge --no-ff`.
- ~~🟢 Higiene: rotar `DISPATCHER_TOKEN` (visible en historial git)~~ **DESCARTADO POR DECISIÓN DE CÉSAR (2026-09-06):** "eso queda así, no pasa nada". No re-proponerlo. (El token lo usan los 3 crons: encuestas, trámites, integridad; el secret sobrante `ZARIS_DISPATCHER_TOKEN` también queda como está.)
- **🟢 Credenciales de prod expuestas — mitigado y LIMPIADO (auditoría 2026-07-18).** La password universal `123456` de 12 cuentas (incl. admins `cesar@` id6 y `roymanos@` id3) estaba publicada en el repo público (guías QA `docs/qa_*.html`, skills, smokes). Se forzó `debe_cambiar_password=true` en las **12** (backup `_backup_debe_cambiar_pwd_2026_07_18` en prod) **y las credenciales se SACARON de todos los artefactos trackeados** (`8efbe1c`): smokes/seeds prod-capaces leen `ZARIS_QA_PASS`/`ZARIS_SEED_*` de env (default dev solo local), guías QA y docs referencian `credenciales-testing/`. **Nota para Roy:** si tu login te pide cambiar la clave, es por esto — normal; y para correr smokes contra prod ahora seteás `ZARIS_QA_PASS`. **Pendiente vivo (decisión de César):** evaluar purga del historial git (las claves viejas siguen en commits pasados; con el cambio forzado ya están muertas).
- ~~🟠 Auditoría de seguridad — último residuo (bypass nivel-2 de Trámites)~~ **CERRADO 2026-07-19** (ver Hecho reciente) — **la auditoría 2026-07 queda 100% completa** (48/48 hallazgos resueltos, refutados o aceptados con decisión). Derivado de DATOS demo — **RESUELTO 2026-08-30**: el agente 91 Juan Pestto (usuario `juanpesto` id 2, nivel 4 Gestión, sin OTs/trámites/aprobaciones/turnos/equipos) pasó de Censos (63) a **Espacios Verdes (74)** (`agentes.id_subarea` y `usuarios.id_subarea` a 74; Censos queda con 2 agentes activos). Con eso los 3 `poda-arbol` vivos ya puede avanzarlos ese usuario, no solo un admin. Revertir = volver ambos campos a 63. Actualizar la planilla de `credenciales-testing/` (lo lista como "Operador" sin subárea). Las demás subáreas usadas por circuitos/visados están pobladas (verificado en prod: 68=1, 72=1, 73=1, 70=2, 1=3, 69=3, 71=4 agentes). Residuos conscientes documentados en la skill `modulo-tramites` (catálogo admin sigue nivel ≤2, lectura sin scope, re-resolver visados).

---

## ✅ Hecho reciente (últimas sesiones)

- **2026-09-01 — 5ª TANDA BI (2 vueltas) + QA DEL BI CERRADO POR CÉSAR — EN PROD** (`64ef616` + `7cfdd16` + dists CI `b7be187`/`d6ddd02`; Railway confirmado por marker OpenAPI; verificado navegando en local Y en el iframe de prod; **OK final de César**). QA de César sobre prod en 2 mensajes → 2 commits: (1ª vuelta) **tablas del módulo sin scroll horizontal** (numéricas a ancho mínimo `width:1%`+nowrap, columna de nombres envuelve) · **Operativo con los defaults y el tratamiento del Ejecutivo**: abre con año en curso + mes anterior + "Todas las áreas" (admin; supervisor su área), "Limpiar" restaura el default completo, período en letras por sección, y `/bi/comparativo` compara contra el **período CONTIGUO anterior** (regla espejada de `_rango_anterior`; key `anio_anterior` queda como nombre legacy; card "Período anterior" con "sin datos en X" y color por valoración) · fix de bug preexistente: `/bi/ejecutivo/top-tipos` pasaba `ant=None` y la columna % Var de los tops mostraba siempre "—". (2ª vuelta) columnas de texto con `overflowWrap:'anywhere'` (único valor que reduce el min-content — con `break-word` la tabla quedaba RECORTADA a la derecha) + margen contra el scrollbar · pastillas de donas **clampeadas al área del chart** + floats a 1 decimal (imprimía "28.400000000000002") · **cover que tapa la franja sobre la barra sticky** (el scroller embebido `main.embeddedContent` con padding-top 16 pega la sticky al borde del contenido y ahí se veía pasar la página). Reglas nuevas en la skill `modulo-bi` (sección "5ª tanda"). **La regla del período anterior ahora vive en TRES puntas espejadas**: `_rango_anterior` ↔ `bi_comparativo` ↔ `periodo.ts`.
- **2026-08-31 — 3ª y 4ª TANDA DEL EJECUTIVO + FIX DASHBOARD + SHELL DEV CLONADO + LIMPIEZA DEMO — EN PROD** (`7f1031c`/`1232c88`/`afacdfa` + dist CI `1dcf2c3`; todo verificado navegando en local y con el bundle de prod confirmado por marker). QA de César sobre prod, resuelto en 4 frentes: (1) **Ejecutivo**: defaults año en curso + mes anterior + "Todas las áreas" (Limpiar vuelve a eso; supervisor n2 conserva su área — "Todas" sigue admin-only, avisar si se quiere para n2); series mensuales con tope 12 meses mostrando solo meses CON datos (`periodo.ts::mesesEjeSerie`); barras sat/cierre sin ceros; mapas con `marcadorPunto` (5px) + `LeyendaMapa` en recuadro + **fit que contiene todos los puntos** — dos causas: `fittedRef` sobrevivía al remontaje del mapa (StrictMode dev) dejándolo en el encuadre default, y el cambio de filtros no re-encuadraba (ahora re-fit por dataset en modo punto + `invalidateSize`/`ResizeObserver`; hook `_mapDebug` en el container para QA por navegador); donas Niveles + Cierres lado a lado en Resumen. (2) **Dashboard home**: el panel flotante ya no captura arrastre ni selección (`pointer-events` selectivos + `user-select: none` — el drag seleccionaba el texto de todas las tarjetas). (3) **Shell DEV clonado de prod** (§14): AppShell standalone ahora replica grid 52/1fr/30, topbar con brand+municipio+contexto 2 líneas+avatar, sidebar `.nav-flat` con secciones/labels de prod, statusbar con salud/reloj/versión; dropdown de notificaciones z2000 (la barra sticky del BI z1100 lo tapaba); User type suma nivel 5 + `cargo_nombre`/`foto_url`. (4) **Incidente cron demo-datos**: el cron del lunes NO estaba muerto — GitHub lo demoró 8 hs (schedule 16:11 UTC); el dispatch manual pedido antes de saberlo apiló una capa más sobre el seed → semana 25-31/8 con ~3x. Limpieza por **conexión directa a la DB de prod** (`backend/.env` + asyncpg — sin MCP): 164 filas (bloque contiguo ids 2258-2421, 100% `generador.demo`) borradas en transacción con backups `_backup_reclamos/reclamo_historial/encuesta_envio/encuesta_respuesta/encuesta_resp_det_dup_2026_08_31` en prod + dump completo local pre-cirugía; dispatch final solo del 31/8 → semana uniforme ~12-18/día, agosto 462. **Reglas nuevas** en skill `modulo-bi` (no re-dispatchar sin verificar datos; generador NO idempotente) y memorias. (`2d22922` + `cc924d2` + `f6f262d`, dist CI `df78cad`; todo verificado navegando en prod con el navegador integrado). Pedidos de César en su QA: (1) **filtro de SUBÁREA** (catálogo `GET /bi/ejecutivo/catalogo/subareas?id_area=`, solo subáreas con reclamos, se resetea al cambiar de área); (2) **período analizado EN LETRAS** al costado del título de cada sección (`lib/periodo.ts::periodoEnLetras`, reemplaza subtítulos); (3) **comparativa redefinida al período INMEDIATAMENTE anterior** — mes 8 vs julio, cruza de año (`pares` año*100+mes en `_where_ej`), año completo vs año-1; arregla el "sin período comparable / anterior: 0" que reportó César (antes comparaba mes 8/2026 vs mes 8/2025 vacío). Regla ESPEJADA backend (`_rango_anterior`) ↔ front (`periodo.ts`): tocar las dos puntas juntas; (4) **matriz sin columna % Resp** (UI+CSV) y **triangulitos ▲▼/■ por indicador** contra `fila.ant` (indicadores del período anterior que ahora devuelve `/matriz`; verde mejora / rojo empeora, días invertido); (5) dona de niveles aclara que el total son encuestas respondidas. Además: **CARTO empezó a exigir API key** (tiles con marca "API KEY REQUIRED", captura de César) → `DashboardMap` y `EmergenciasMap` pasaron a **Esri World Light/Dark Gray Base** (sin key, `maxNativeZoom` 16; regla actualizada en CLAUDE §4 y §13) · fix: el Ejecutivo mostraba el título del Operativo (`BiLayout` parametrizado). El reporte inicial "tableros vacíos" resultó ser caché del iframe apuntando al asset con hash viejo (borrado por el deploy) + filtros guardados.
- **2026-08-30 (4ª sesión) — GENERADOR DE DATOS DEMO BI + CRON SEMANAL — EN PROD** (`55c7bf1`). Pedido de César: poblar el sistema con ~300-500 reclamos/mes desde abril usando como referencia los datasets reales de VL que dejó en `SMOKE/` (gestión de mayo, 3.888 filas + export Qualtrics de encuestas, 23.419 respuestas; carpeta **gitignoreada — datos personales reales, jamás versionar**). Entregado: (1) `app/services/demo_datos.py` + `demo_datos_ref.py` — genera reclamos+historial+encuestas CSAT con las distribuciones de VL (tipos matcheados POR NOMBRE contra nuestro catálogo — decisión de César: mapear al catálogo de San Andrés, no importar el de VL; localidades con centroides y jitter; estados 73/11/11; demora lognormal mediana 3 p90 14; score Q1 50%-cinco → %sat ~68-69; nombres RECOMBINADOS del CSV, nunca el par real; DNI/CUIL sintéticos válidos). Todo reclamo ligado a un ciudadano (pool 610 existentes + ~250 vecinos demo `@vecinos-demo.zaris.com.ar`) y TODO atribuido al usuario INACTIVO `generador.demo` → **rollback o migración multi-tenant = un WHERE por ese `id_usuario_alta`** (decisión de César: IT-01 DESPUÉS del seed). Regla crítica: los `encuesta_envio` demo nunca nacen `pendiente` (el dispatcher los mandaría por mail); solo enviada/completada/expirada. (2) `POST /api/v1/demo/poblar` (auth dual: X-Dispatcher-Token o JWT admin; rango máx 45 días) + `avanzar_pendientes` que envejece SOLO lo demo (asigna/resuelve/cancela/madura encuestas). (3) `.github/workflows/demo-datos.yml` — cron LUNES 08:20 UTC + dispatch manual con rango (usa el secret `ENCUESTAS_DISPATCHER_TOKEN` existente). (4) Carga inicial: local vía `seed_demo_bi.py` (1.565 reclamos + 25 sub, 1.238 envíos, ~430 respuestas; smoke API local: score 90,9% cierre / 55% SLA / 69,3% sat, top 1 "Falta de barrido" igual que VL, 401 sin auth) y prod vía dispatches del workflow mes a mes (backups previos `_backup_reclamos_pre_demo_2026_08_30` + `_backup_encuesta_envio_pre_demo_2026_08_30`). Drift §24 cazado: `usuarios.id_municipio`/`buc_acceso` y `ciudadanos.ren_chk/email_chk/emp_chk/activo` NOT NULL sin default SOLO en local. (5) Fix visual del Resumen Ejecutivo: matriz a lo ancho, dona abajo. **Para Roy:** los ~1.500 reclamos y ~860 vecinos demo de prod son datos sintéticos del generador — no los borres ni los uses de padrón real; identificables por `id_usuario_alta = generador.demo` / email `@vecinos-demo.zaris.com.ar`.
- **2026-08-30 — BI EJECUTIVO "Análisis de demanda ciudadana" — EN PROD** (`d770f6b` + dist CI `c920c2b`; deploy Railway confirmado por marker OpenAPI `/bi/ejecutivo/score`; GH Pages sirve `index-BV7ECzp4.js`; smoke 18/18 local + 10/10 read-only en prod con `administrativo` n2). Réplica ZARIS de los 5 tableros Power BI de VL que pasó César (capturas). Decisiones suyas: **página única** patrón Operativo (índice fijo + scroll-spy + filtros contraíbles) · filtros **período + área (+ localidad)** · **desglose por SUBÁREA** (las "áreas de servicio" de VL son nuestras subáreas) · localidad = **backfill desde lat/lon + captura en el form**. 5 secciones: Resumen (score %cierre/%SLA/%sat, niveles 1-5 sin emoji, matriz subárea→tipo expandible con export CSV) · Evolución (altas vs cierres, indicadores mensuales, dona Cumplido/Auditado vía historial) · Histórico (apilados por subárea/canal/localidad + donas) · Mayores (top 10 por cantidad y por demora) · Satisfacción (barras sat vs cierre + 2 mapas). Backend `bi_ejecutivo.py` (11 endpoints, guard JWT router-level). **Localidad:** backfill por reverse geocoding en prod 39/45 (Olivos 14 · Florida 13 · VL 9 · La Lucila 2 · Munro 1; 6 fuera del partido quedan NULL; backups `_backup_reclamos_localidad_2026_08_30` local+prod) + derivación automática en los 3 creates (`geo.py::localidad_desde_coords`, match POR NOMBRE — los ids de partido divergen entre entornos) + campo readonly autoderivado en el FormView de Reclamos. **Para Roy:** los reclamos que la PWA crea con GPS ahora quedan con `id_localidad` solos — sin cambio de contrato. Sin migración de schema (columna ya existía). Reglas vivas en skills `modulo-bi` y `modulo-reclamos`. **QA visual en prod pendiente de César** (ver Pendientes).

- **2026-08-30 — BI Operativo en UNA página — EN PROD** (`ad7aa5d` + dist de CI; deploy Railway confirmado por marker `/bi/mi-area`; verificación read-only en prod con `administrativo` n2: área por defecto 22 "Servicios Públicos" desde su agente, filtros estado/canal, histograma por tipo, export del universo con `X-Total-Count`, `dias_demora` en el geo). Decisiones de César tras revisar los 4 tableros Power BI de Vicente López (AskUserQuestion): **una sola página** con índice fijo en vez de 4 tabs · **Subreclamos como sección propia al pie** · **filtros globales** = fechas con atajos de año/mes + área + prioridad + **estado + tipo (buscador) + canal** · **histórico con toggle Estado/Tipo** (top 6 tipos + Otros) · y, pedido en el momento, **el área de servicio como selector principal con una por defecto** ("estas vistas son para cada área"): Supervisor → la de su agente (`GET /bi/mi-area`, regla §3, solo áreas activas), Admin → la última usada o la sugerida, "Todas" solo admin. **Sin tablas de detalle: cada sección exporta los tickets filtrados** (Resumen exporta el universo vía `/bi/reclamos-detalle`). Mapa de pendientes con semáforo de demora. Backend: filtros `estado`/`id_tipo_reclamo`/`canal` aplicados por `_aplicar_extras` en los 28 endpoints + 4 nuevos. Frontend: `OperativoPage` + `sections/*` + `FiltrosGlobales` + `TipoSearch`; las rutas viejas por tab siguen y desplazan a la sección. Verificado navegando en local (área por defecto, chips, buscador, cambio de área, Limpiar, toggle Tipo, export → 200). **Ajustes de César tras verlo en prod (misma tarde):** la barra fija muestra siempre la **sección activa** (scroll-spy) y al saltar desde el índice el título queda debajo de los filtros · **KPIs en UNA sola fila** por sección, siempre con el totalizador de lo filtrado + **promedio mensual del último año** + **mismo período del año anterior** con variación (endpoint nuevo `GET /bi/comparativo`), Respuesta con tiempo de cierre promedio y % SLA · panel **"Filtrado de análisis" en naranja** · **meses como tildes independientes + "Seleccionar año completo"** (filtros `anio`/`meses` en los 29 endpoints). **2ª tanda de ajustes (`d76fffc`, misma noche):** panel de filtros **contraíble** (contraído muestra área · período · filtros; preferencia en localStorage) · meses **sin tilde** · **índice corregido** (marcaba siempre la sección anterior: el spy ahora compara contra el borde inferior real de la barra + debounce de cola + re-evaluación al vencer el lock del salto) · **mapa de Pendientes ya no tapa la barra** (sticky z 1100 + `isolation:isolate` en el wrapper del mapa) · período legible **"2026 · año completo" / "mes 5" / "meses 5, 6"** (backend `/bi/comparativo` + front) · **total en cantidad al centro de las 6 donas** (`DonaCentro`) · doble separación entre visualizaciones (no entre tarjetas). Marker OpenAPI: 422 de `/bi/comparativo`. **QA visual en prod: OK de César (3ª vuelta, misma noche).** Reglas vivas en la skill `modulo-bi`.
- **2026-08-30 — Foto de perfil del vecino — EN PROD** (`2af84c3`; mig **102** `ciudadanos.foto_path`/`foto_actualizada_en` en local y prod; deploy confirmado por marker OpenAPI; `smoke_publico_perfil_avisos.py` **34/34 local y prod**, con subida real al bucket). Esquema de Roy aprobado por César: bucket privado de adjuntos, path fijo `perfiles/{id_ciudadano}.jpg` con upsert (1 foto, sin huérfanos), solo JPEG 1 KB..512 KB y ≥100×100 px (dimensiones leídas del SOF, sin sumar Pillow a requirements), `foto_url` firmada TTL 1 h en `GET /perfil`, `DELETE` idempotente, rate-limit 5/min. **Para Roy:** punto 4 del aviso en "En curso" (la PWA deja el `localStorage`).
- **2026-08-30 — Ciclo de vida del trámite hacia el vecino — EN PROD** (`78562c8` + dist CI `894a266`; migs **101** DDL y **101b** seed en local y prod; deploy Railway confirmado por marker OpenAPI; smoke `smoke_tramites_ciclo_vida.py` **23/23 local** — solo local porque viaja en el tiempo por SQL; verificación read-only en prod). Decisiones de César (AskUserQuestion): standby = **flag por estado** `espera_iniciador` · cronograma **+30 / +60 / 72 h antes / desistido a +90** (configurable) · desistido = **marca paralela** `resultado='desistido'` + `archivado_motivo='desistimiento'` · **resultado derivado de la transición final** (`aprobar`→aprobado, `rechazar`→rechazado). Entregado: `services/tramites/ciclo_vida.py` (resultado auto en la misma transacción; `al_terminar` post-commit = aviso PWA `tramite_estado` + push + encuesta CSAT de trámites; `procesar_desistimientos` en el cron diario = avisos `tramite_pendiente` bandeja+push+**mail** + movimiento `aviso_iniciador` + notificación interna al colectivo "contactalo", y DESISTIDO automático con movimiento `desistido`) · `tipo_tramite.sla_dias` + `tipo_tramite_estado.espera_iniciador` en el builder (SLA en Editar tipo; flag en el modal de estado vía `PATCH /admin/tramites/estados/{id}/espera-iniciador`, editable en versiones publicadas) · `POST /resultado` acepta `desistido` (manual) · `ResultadoChip` y timeline · encuestas: 4ª rama `id_tramite` + plantilla "Encuesta de satisfacción de trámites" + render de mail · `archivar_inactivos` excluye los estados que esperan al vecino · 6 claves nuevas en Config → Sistema. **Para Roy:** los avisos de trámites llegan a la bandeja con `tipo` `tramite_estado`/`tramite_pendiente` y `url=/alertas` (contrato §11). **Para César:** ver el pendiente "CONFIGURAR EL CATÁLOGO".
- **2026-08-30 — Backend para la PWA: perfil + bandeja de avisos + reverse geocoding — EN PROD** (`47ebbe1` + `51ca34f`; migs **99** `ciudadano_aviso` y **100** `ciudadanos.telefono` VARCHAR(15) aplicadas en local y prod; deploys Railway confirmados por marker OpenAPI; smoke `smoke_publico_perfil_avisos.py` **27/27 local y 27/27 prod**). (1) `publico_perfil.py`: `GET` ficha completa de la BUC con el CUIL placeholder enmascarado, `PUT` parcial solo de contacto/domicilio (allowlist; identidad y email fuera). (2) `publico_avisos.py` + `services/avisos.py`: los hooks post-commit de `push.py` persisten el aviso ANTES del push → cubre reclamos.py + ordenes_trabajo.py + emergencias.py sin tocar routers; `GET` con contadores, `PATCH /{id}/leer`, `POST /leer-todos`, `mi-resumen.avisos.no_leidos`. (3) `GET /publico/reclamos/geo/reverse` con helper `reverse_geocodificar` extraído del `/geo/reverse` del backoffice (compat). **Drift cazado en prod:** `ciudadanos.telefono` era VARCHAR(10) (local 15) → el primer smoke prod dio 500 en el PUT → mig 100 + normalización digit-only con la regla del BUC + `DataError`→422. Datos demo que quedaron en prod: reclamos `id 66` y `67` del Vecino Demo (el 67 pasó a En gestión por `administrativo`, generó el aviso #1). Contrato §4/§5/§10/§11, HISTORIAL migs 99-100, skill `modulo-app-vecinos` y CLAUDE §21 actualizados. **Para Roy:** ver el aviso en "En curso".
- **2026-08-30 — Retomada + quick wins (condensado):** color primary PWA `#1e5aa8` · Pestto a Espacios Verdes (destraba poda-árbol demo) · prefijo pánico documentado en contrato §8 · Vercel team Hobby (sumar a Roy exige Pro) · Supabase sigue `us-east-1`. Detalle en la bitácora e `HISTORIAL_MIGRACIONES.md`.
- **2026-07 (resumen condensado, detalle en `HISTORIAL_MIGRACIONES.md` y la bitácora):** 2026-07-19 cierre TOTAL de la auditoría 2026-07 (bypass n2 de Trámites scopeado, 48/48) · 2026-07-18 maratón: anti-carrera TOCTOU (mig 95), scope-subárea en mutaciones (`ff24619`), renovación de credenciales (mig 96), alerta de pánico (mig 97), encuestas de Entradas (migs 98+98b), guards anti-lockout, credenciales fuera de artefactos trackeados (`ZARIS_QA_PASS`) · 2026-07-16 PWA de Roy a prod + Config 2 tabs + Usuarios migrado a React · 2026-07-15 IDOR alta de empresa cerrado + regla "empresa exige vecino" + testing de Roy (ClickUp) resuelto + IT-02..06.
> Detalle histórico completo: `HISTORIAL_MIGRACIONES.md` + tabla "Histórico breve" de la bitácora.

---

## 🔗 Mapa de fuentes de verdad (para no buscar a ciegas)

| Querés saber… | Mirá… |
|---|---|
| Reglas de cómo se trabaja (mandatorias, transversales) | [`CLAUDE.md`](CLAUDE.md) |
| Detalle de UN módulo (tablas/endpoints/FSM/quirks) | la skill `.claude/skills/modulo-<nombre>/` — carga sola al tocar ese módulo (ver `PLAN_REFACTOR_SKILLS.md`) |
| Estado de avance / pendientes HOY (backoffice) | **este archivo** |
| Estado de avance / pendientes HOY (PWA) | `zaris-vecinos/ESTADO.md` |
| Roadmap detallado de la App Vecinos | [`PLAN_APP_VECINOS.md`](PLAN_APP_VECINOS.md) |
| Roadmap detallado de Emergencias | [`PLAN_MODULO_EMERGENCIAS.md`](PLAN_MODULO_EMERGENCIAS.md) |
| Qué migración hace qué / cuándo se aplicó | [`HISTORIAL_MIGRACIONES.md`](HISTORIAL_MIGRACIONES.md) |
| Contrato de la API pública que consume la PWA | [`docs/contrato_api_publica.md`](docs/contrato_api_publica.md) |
| Cómo arrancar local / colaborar | [`ONBOARDING.md`](ONBOARDING.md) · [`CONTRIBUTING.md`](CONTRIBUTING.md) |
| Quirks/patrones/decisiones (foto de las memorias) | [`docs/memoria-proyecto/`](docs/memoria-proyecto/) |
